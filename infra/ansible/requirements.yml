---
collections:
  # Pinned to the 3.x major (open floor, capped ceiling); bump the ceiling
  # deliberately after checking the 4.x changelog for breaking changes.
  - name: community.postgresql
    version: ">=3.0.0,<4.0.0"
  # authorized_key module — provisions arxadmin's SSH keys (base role).
  - name: ansible.posix
    version: ">=1.5.0,<3.0.0"

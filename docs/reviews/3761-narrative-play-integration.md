# Review evidence

- Reviewed revision: `57c82b9105cea6f6a94eb912c78a0bb03b4ce4a6`
- Reviewer: Claude Sonnet 5 (Task 8, #3761 evidence ledger; fix round 1 closed the A17 gap and converted A03/A09/A19 from OUT_OF_SCOPE to live-verified rows)
- Reviewer verdict: PASS
- Application/build identity: Evennia Portal/Server 5.0.1, Arx Portal/Server banner reports "rev 57c82b910" (matches Reviewed revision); frontend built via `pnpm build` from this same commit and served through `arx manage collectstatic` to `src/server/.static`, run with `arx start` (Django/Twisted on port 4001, websocket on 4002).
- Environment: Local devcontainer, real running app (not mocked) against a freshly built, empty PostgreSQL database (`test_arxiidev`, built via `just build-test-schema` from current model state, isolated from the shared `arxiidev` dev database) — chosen because `arxiidev` was 5 migrations behind this branch's model state and this task's charter forbids running `migrate` against the shared dev database. Login performed as a real seeded test account (`e2e_test_account`, created via `evennia.utils.create.create_account`, the same production account-creation path `ArxAccountAdapter.new_user` uses) with one real playable character (`EvidenceWalker`) and a real `RosterTenure`, driven with a genuine browser (Playwright-launched headless Chromium, `frontend/pw_driver.mjs`, a throwaway driver script written for this task and not committed) hitting the real backend over real HTTP/WebSocket — no `page.route()` mocking anywhere in this evidence.
- Viewports/themes: Desktop 1280x900 (light theme, the viewer default) for the primary journeys; 375x800 (mobile) for the responsive spot-check (A18). Dark theme and 200% zoom were not exercised this pass (see Unresolved findings — none outstanding, this is scoped explicitly in the A18/A03 ledger rows below).
- Approved design: `docs/design-proposals/narrative-play/arx-play-specification.md` at commit `c183f42773d8a4c78b077f0cc5a11f2acd2ff62f` (fetched via `git show`; this spec is a written specification, not a Figma/demo page, so "visual checklist" below matches rendered UI against the spec's described elements/required-results rather than pixel comparison to a mockup).
- Visual review: Completed — live screenshots taken directly from the running app for each checked element, compared against the spec's section 4 (sidebar), section 8 (composer/actions) and section 14 (acceptance matrix) descriptions.
- Visual verdict: PASS
- Screenshots: ![A01 account home, one real character](docs/reviews/assets/3761/a01-account-home-character-card.png) ![A01/A02 real game entry into Limbo](docs/reviews/assets/3761/a01-a02-game-entry-limbo-room.png) ![A02 a real scene started](docs/reviews/assets/3761/a02-scene-started.png) ![A17 combat banner + rail live](docs/reviews/assets/3761/a17-combat-banner-and-rail.png) ![A14/A17 pose sent while in combat](docs/reviews/assets/3761/a14-a17-pose-sent-during-combat.png) ![A17 History mode reachability](docs/reviews/assets/3761/a17-history-mode-reachable.png) ![A17 draft survives jumpToCombat](docs/reviews/assets/3761/a17-draft-survives-jump.png) ![A17 aftermath dismiss-outcome](docs/reviews/assets/3761/a17-dismiss-outcome.png) ![A20 channels-unavailable note](docs/reviews/assets/3761/a20-channels-unavailable-note.png) ![A18 mobile 375px viewport](docs/reviews/assets/3761/a18-mobile-375-viewport.png) ![A18 mobile sidebar panel](docs/reviews/assets/3761/a18-mobile-sidebar-panel.png) ![A03 independent scroll before mode switch](docs/reviews/assets/3761/a03-independent-scroll-before-switch.png) ![A03 independent scroll after mode switch](docs/reviews/assets/3761/a03-independent-scroll-after-switch.png) ![A09 search beyond 90 days with explicit range](docs/reviews/assets/3761/a09-history-search-beyond-90-days.png) ![A09 default 90-day window finds nothing](docs/reviews/assets/3761/a09-default-window-no-match.png) ![A19 corrupted storage renders a safe, non-crashing composer](docs/reviews/assets/3761/a19-corrupted-storage-safe-default.png) ![A19 second account shows no leaked draft](docs/reviews/assets/3761/a19-second-account-no-draft-leak.png)
- Comparison notes: Every screenshot above was captured against the real running app on this revision, not a demo/static mock. The combat banner text ("In combat: your turn"), sidebar nav icon (red "Combat" label), CombatRail tabs (Your Turn / Map / GM) and the room composer all match the spec's section 4/8 descriptions and Task 1-7's own PR descriptions of what was built. The "OOC channels unavailable, pending #3299." note matches the umbrella issue's stated alternate acceptance path for A20 verbatim. Fix round 1 (this revision's evidence commit) added: the sidebar's nav bar (`aria-label="Sidebar modes"`) rendering the "Combat" button and the top-bar banner simultaneously with History mode's own body, the composer's typed-but-unsent value surviving a full History→jump-to-combat→Conversations round trip verbatim, the `EncounterOutcomeBanner`'s "Encounter Concluded" heading and `data-testid="aftermath-dismiss"` button actually rendering and clearing the rail on click, the History sidebar's search-result list scrolling independently of the main feed, a real 120-day-old planted pose surfacing only with an explicit `From` date (and a negative control confirming it is absent under the unmodified 90-day default window, "No accessible matches."), and a corrupted `sessionStorage` draft entry failing safely (no page crash, no thrown JS error) without leaking into a second, freshly-switched account's own composer.
- Tested interactions: (1) Fresh login at `/login` with a real account, landing on the account home page showing exactly one real playable character card. (2) Selecting that character and navigating to `/game`; the app opened a real WebSocket to `ws://localhost:4002/ws/game/`, sent a real `@ic EvidenceWalker`, and received real `puppet_changed`/`room_state` frames placing the character in the real `Limbo` room (id `#2`) with zero scene attached — room panel rendered fully (description, 0 other characters, 1 object, no exits) with no scene id present, matching A02's "room remains complete without a scene ID." (3) Clicking "Start Scene" created a real `Scene` row via the real REST endpoint. (4) Typing a pose and sending it with Control+Enter when NO scene was active correctly failed server-side ("Command ... is not available") — confirmed this is by design (pose submission is scene-scoped), not a defect, by repeating the same action with an active scene, where it succeeded via a real `POST` to the pose-submission endpoint and echoed back over the WebSocket as a real `interaction` frame, rendering in the reading pane. (5) Created a real `CombatEncounter` + `CombatParticipant` (via Django ORM/factories, not REST, since there is no UI path to start combat without a full encounter-design flow this task is not building) attached to the real, just-started `Scene`, and observed the frontend's own 15s poll (`useEncounterForScene`) pick it up with zero page reload: the top-bar "In combat: your turn" banner and the sidebar's red "Combat" nav label both appeared live, while the center pane kept showing the ordinary Conversations reading view the whole time. Clicking the banner and separately sending an ordinary pose both worked with the CombatRail still mounted in the sidebar throughout. (6) Opened the sidebar's Conversations tab and confirmed the literal "OOC channels unavailable, pending #3299." note renders, with only the real "Room" conversation entry listed (no fabricated channel list, no second backend). (7) Resized the viewport to 375px width, confirmed the mobile sidebar toggle opens/closes a real bottom-tab-driven panel with all of Here/Conversations/History reachable and no page-level horizontal overflow. (8, fix round 1) With a real, live `CombatEncounter` active, switched the sidebar to History mode: the top-bar banner and the sidebar's own "Combat" nav button both stayed visible and clickable; clicking "Combat" from History routed back to Here mode's Room tab with `CombatRail` mounted (`aria-current="page"` moved to the "Here" nav button). (9, fix round 1) Typed an unsent pose into the composer while in Conversations mode, clicked the banner to `jumpToCombat()` (sidebar switched to Here/Room with `CombatRail` visible, composer value unchanged), then switched back to Conversations — the exact typed string was still present, unsent. (10, fix round 1) Ended the same live encounter through the real `POST /api/combat/{id}/end/` REST action (authenticated as a second, GM-flagged account created for this check, `IsEncounterGMOrStaff`-gated) rather than a raw database write, because `CombatEncounter` is a `SharedMemoryModel` (idmapper) and a direct out-of-process `.save()` was empirically confirmed to leave the live Evennia server process's own cached instance of that row stale (the frontend kept polling it as still-active for 20+ seconds) — routing the completion through a REST call the live process itself handles updates that same in-memory instance, which the next poll then correctly reflects; within seconds the `EncounterOutcomeBanner` ("Encounter Concluded: Round 0", outcome "Abandoned") rendered with a `data-testid="aftermath-dismiss"` button, clicking it fired `handleDismissOutcome` (which invalidates the `combatKeys.encountersForScene` query), and the banner/rail cleared shortly after. (11, fix round 1) Sent 25 real poses through the composer to give the main reading pane (`data-testid="feed-scroll-container"`) enough transcript to overflow; scrolled it, then switched to History mode and searched for the filler marker (`GET /api/play/search/`, 30 real matching rows), scrolled the sidebar's own result list independently, and confirmed via `element.scrollTop` reads that scrolling one container never moved the other, and that switching away from and back to History restored its own remembered scroll offset exactly. (12, fix round 1) Planted one real pose with a distinctive phrase, backdated its `timestamp` to ~120 days ago via a raw DB `.update()` (safe here because this specific row had never been read by the live server process before, so its first-ever fetch reads the correct backdated value with no idmapper staleness risk), then searched for that phrase twice: with an explicit `From` date before the backdated timestamp it was found (1 result, correct excerpt) and — as a negative control proving the explicit range is what matters, not an unbounded search — with the default unmodified 90-day window it was correctly absent ("No accessible matches."). (13, fix round 1) Typed an unsent draft, corrupted its `sessionStorage` entries (both the current and legacy draft-storage key formats) with invalid JSON, and navigated back into `/game`: the app did not crash and threw no JS error; the composer restored the literal corrupted string verbatim, which on further investigation is correct behavior (the legacy `v1` key is a plain-text cache, not JSON — see the corrected A19 ledger row). Then logged out via the real header "Logout" control and logged into a second, distinct account with its own character: that account's composer started empty (no leaked account-1 text), even though account 1's raw `sessionStorage` entries were still technically present in the same browser tab, because the draft storage key format (`arx:play-draft:v2:` followed by accountId:personaId:conversationKey) is scoped by account id and account 2's own code never reads account 1's key.
- Fixture/live boundary: The account, character, roster tenure, scene, combat encounter and combat participant were all created as real rows via Django ORM/FactoryBoy calls directly against the running app's own database (`test_arxiidev`) — not through `page.route()` mocks, not through fixture JSON loaded into the frontend. Every screenshot reflects the real backend's real HTTP/WebSocket responses to a real browser. The one exception: the combat encounter itself was created via a backend shell script rather than a UI-driven "start combat" flow, because no such UI entry point exists to trigger combat outside of actual game mechanics this task is not authorized to exercise (starting a real fight) — the reachability wiring under test (banner/nav-icon to `CombatRail`) is identical either way, since both paths converge on the same `GET /api/combat/?scene=SCENE_ID` poll Task 1-4 built. Fix round 1 additionally created: a second real account+character (`e2e_fix1_account2`/`SecondWalkerFix1`) for the A19 cross-account check, a third real account flagged `is_staff=True` (`e2e_fix1_gm`) used only to call the real `end` REST action as a GM, and ~55 real `Interaction` rows (via direct ORM `Interaction.objects.create`/`.update()` calls and, for most of the A03 filler content, real composer pose submissions) to give the reading pane and search index enough real content to exercise scrolling and date-bounded search. `sessionStorage` corruption (A19) and encounter-completion timing (A17) were driven directly via `page.evaluate`/a real second authenticated REST call, never `page.route()` mocking.
- Overall outcome: PASS

## Visual checklist

| Element | Expected | Result | Evidence |
| --- | --- | --- | --- |
| Account home character list | Exactly the real characters this account has a tenure for, no placeholder rows | MATCH | `docs/reviews/assets/3761/a01-account-home-character-card.png` |
| Game entry room panel (no scene) | Room fully renders (description, characters, objects, exits) without requiring a scene id | MATCH | `docs/reviews/assets/3761/a01-a02-game-entry-limbo-room.png` |
| Scene start affordance | "Start Scene" creates a real, named scene shown in the sidebar | MATCH | `docs/reviews/assets/3761/a02-scene-started.png` |
| Top-bar combat banner | Red "In combat: your turn" banner, dismissible, reachable while center pane shows ordinary conversation | MATCH | `docs/reviews/assets/3761/a17-combat-banner-and-rail.png` |
| Sidebar Combat nav + CombatRail | Red "Combat" nav label plus Your Turn / Map / GM tabs render in the sidebar simultaneously with the Conversations reading pane | MATCH | `docs/reviews/assets/3761/a17-combat-banner-and-rail.png` |
| Ordinary pose during active combat | Composer accepts and sends a plain pose while the combat banner/rail stay mounted | MATCH | `docs/reviews/assets/3761/a14-a17-pose-sent-during-combat.png` |
| Channels gap note | Literal "OOC channels unavailable, pending #3299." note in the Conversations tab, only the real Room conversation listed | MATCH | `docs/reviews/assets/3761/a20-channels-unavailable-note.png` |
| Mobile 375px layout | No page-level horizontal overflow, sidebar reachable via a bottom toggle | MATCH | `docs/reviews/assets/3761/a18-mobile-375-viewport.png` |
| Mobile sidebar panel | Here/Conversations/History tabs and room detail all legible and reachable at 375px | MATCH | `docs/reviews/assets/3761/a18-mobile-sidebar-panel.png` |
| History mode reachability | Top-bar combat banner and sidebar "Combat" nav button both stay visible while History mode's own body is shown | MATCH | `docs/reviews/assets/3761/a17-history-mode-reachable.png` |
| Draft survives jumpToCombat | Unsent composer text is byte-identical after Conversations to Here to Conversations round trip | MATCH | `docs/reviews/assets/3761/a17-draft-survives-jump.png` |
| Aftermath dismiss-outcome | `EncounterOutcomeBanner` ("Encounter Concluded") with a working Dismiss button renders once the encounter completes | MATCH | `docs/reviews/assets/3761/a17-dismiss-outcome.png` |
| Independent scroll retention | Main feed and sidebar search results each keep their own `scrollTop`; switching sidebar modes restores the sidebar's own offset | MATCH | `docs/reviews/assets/3761/a03-independent-scroll-before-switch.png` |
| History search beyond 90 days | An explicit `From` date before a 120-day-old pose's timestamp retrieves it with correct excerpt | MATCH | `docs/reviews/assets/3761/a09-history-search-beyond-90-days.png` |
| History search default window (negative control) | The same phrase is absent ("No accessible matches.") under the unmodified 90-day default | MATCH | `docs/reviews/assets/3761/a09-default-window-no-match.png` |
| Corrupted storage safe handling | No crash/thrown error on a corrupted draft entry; app remains usable (composer echoes the raw string, which is correct — the v1 draft key is plain text, not JSON; see A19 row) | MATCH | `docs/reviews/assets/3761/a19-corrupted-storage-safe-default.png` |
| No cross-account draft leak | A freshly-switched second account's composer starts empty despite account 1's raw storage entries still existing in the same tab | MATCH | `docs/reviews/assets/3761/a19-second-account-no-draft-leak.png` |

## Unresolved findings

None

## Requirement ledger

| ID | Status | Evidence | Authorized decision |
| --- | --- | --- | --- |
| A01 | PASS | Real login (`e2e_test_account`) -> account home shows exactly one real character card -> select -> `/game` opens a real WebSocket, sends real `@ic`, receives real `puppet_changed`/`room_state`, reaches "In world" with no account transcript shown to the player. Screenshots: `docs/reviews/assets/3761/a01-account-home-character-card.png`, `docs/reviews/assets/3761/a01-a02-game-entry-limbo-room.png`. | The induced-failure/retry sub-case (an account with zero characters, or a rejected `@ic`) was not separately re-triggered live this pass beyond the incidental failure hit and fixed during test-account setup (an account-creation-path defect in this task's own throwaway test fixture, not a product defect — see Tested interactions notes in the task report); the successful-entry path is fully proven live. |
| A02 | PASS | Limbo room panel renders fully (description "This is a room.", Characters (0), Objects (1) listing "bootstrap", Exits "No obvious exits") with zero scene attached — room is complete without a scene id. Travel/Codex/Journal/Items nav entries are present and clickable in the same panel. Screenshot: `docs/reviews/assets/3761/a01-a02-game-entry-limbo-room.png`. | Object inspection and an actual travel action (walking through an exit) were not exercised live this pass — Limbo has no exits in this fresh database, so a travel journey needs a second room/exit this task did not build. |
| A03 | PASS | (Fix round 1, live) Sent 25 real poses via the composer so the main feed (`data-testid="feed-scroll-container"`) had enough transcript to overflow; scrolled it to `scrollTop=3541`. Switched the sidebar to History mode and searched the filler marker (`GET /api/play/search/?q=Fix1-scroll-marker&from=2020-01-01`, 30 real matching rows), then scrolled the sidebar's own result list (`data-testid="play-sidebar-scroll"`) to `scrollTop=2712` — the main feed's `scrollTop` stayed exactly `3541` throughout, proving the two containers scroll independently with no accidental outer-transcript scroll. Switching sidebar mode away (Here) and back to History restored the History body's own `scrollTop=2712` exactly, per-mode retention working as designed (`PlaySidebar.tsx`'s `scrollPositionsRef`). Screenshots: `docs/reviews/assets/3761/a03-independent-scroll-before-switch.png`, `docs/reviews/assets/3761/a03-independent-scroll-after-switch.png`. | Not part of Tasks 1-7's delivered diff (no scrolling/pagination code changed on this branch) — this re-verifies existing, unmodified behavior rather than anything shipped this round. |
| A04 | OUT_OF_SCOPE | n/a | Requires seeding 500 poses across 20 threads with colliding timestamps, disproportionate to a live spot-check session and outside Tasks 1-7's diff. Pagination logic is unchanged by this branch. |
| A05 | OUT_OF_SCOPE | n/a | No reply/thread history existed on the fresh test scene; threading logic is unchanged by Tasks 1-7's diff. |
| A06 | OUT_OF_SCOPE | n/a | Needs an existing collapsed thread receiving 20 live replies; not set up this pass and unchanged by this branch's diff. |
| A07 | OUT_OF_SCOPE | n/a | Needs a folded thread with a mix of read/unread poses; not set up this pass and unchanged by this branch's diff. |
| A08 | OUT_OF_SCOPE | n/a | Needs multiple pages of history above the viewport to test scroll-anchor restore; the fresh test scene had one pose. Unchanged by this branch's diff. |
| A09 | PASS | (Fix round 1, live) Planted a real pose ("Quennelvix-tarnhollow marker...") and backdated its `timestamp` to `2026-05-15` (~120 days before the reviewed session's `2026-09-12`) via a targeted DB `.update()`. Searching History with an explicit `From=2026-01-01` returned exactly that pose (`GET /api/play/search/?q=Quennelvix-tarnhollow&from=2026-01-01` → 1 result, correct excerpt, `scene:2`). Negative control: the identical search with no explicit `From` (the component's own unmodified 90-day default, resolving to `from=2026-06-14`) correctly returned zero results and rendered "No accessible matches." — proving the explicit date range, not an unbounded search, is what retrieves the retained result, matching this gate's required result exactly ("Explicit date range retrieves retained authorized result"). Screenshots: `docs/reviews/assets/3761/a09-history-search-beyond-90-days.png`, `docs/reviews/assets/3761/a09-default-window-no-match.png`. | The backdated row was never read by the live server process before the update (created and backdated in the same out-of-process script, before any request touched it), so this sidesteps the `SharedMemoryModel` idmapper staleness risk documented on the A17 row below — confirmed by a fresh `arx shell` `.get()` and raw SQL both reading the correct backdated value before the live app ever queried it. |
| A10 | OUT_OF_SCOPE | n/a | Needs a second persona and an existing whisper thread plus an in-progress room draft; not set up this pass and unchanged by this branch's diff. |
| A11 | OUT_OF_SCOPE | n/a | Needs a deliberately deleted/blocked parent pose and a stale client-side reference; not set up this pass and unchanged by this branch's diff. |
| A12 | OUT_OF_SCOPE | n/a | Needs a character handoff (old tenure ended, new tenure begun) with pre-existing private whispers; not set up this pass and unchanged by this branch's diff. |
| A13 | OUT_OF_SCOPE | n/a | Needs multiple personas with differing language/permission grants and muted/VERY_PRIVATE content; not set up this pass and unchanged by this branch's diff. |
| A14 | PASS | Sent a real pose via the room composer (Control+Enter) while a real scene was active; it POSTed to the real pose-submission endpoint and echoed back over the WebSocket as a real `interaction` frame, rendering immediately in the reading pane with correct author/timestamp. Screenshot: `docs/reviews/assets/3761/a14-a17-pose-sent-during-combat.png`. | Send-lose-ack-retry-echo and "newer draft not cleared" specifically were not independently fault-injected this pass (would need request interception, which this task's evidence is deliberately avoiding as the primary proof mechanism). |
| A15 | OUT_OF_SCOPE | n/a | Needs a request held in flight across a scene/room/persona change, which requires network-timing control; not exercised live this pass and unchanged by this branch's diff. |
| A16 | OUT_OF_SCOPE | n/a | Needs an ephemeral scene with 200+ poses plus a reload/reconnect; disproportionate to this live spot-check session and unchanged by this branch's diff. |
| A17 | PASS | Created a real `CombatEncounter`+`CombatParticipant` attached to a real, just-started `Scene`; the frontend's own 15s poll (`useEncounterForScene`, unmodified by this task) picked it up live with no reload. Top-bar "In combat: your turn" banner and sidebar red "Combat" nav label both appeared while the center pane still showed the ordinary Conversations view (Task 7's exact concern); clicking the banner kept the CombatRail (Your Turn/Map/GM tabs) mounted; an ordinary pose sent while the rail stayed open worked normally. Screenshots: `docs/reviews/assets/3761/a17-combat-banner-and-rail.png`, `docs/reviews/assets/3761/a14-a17-pose-sent-during-combat.png`. **Fix round 1 closed the three gaps a task reviewer found in this row's original evidence** (it covered only Conversations-mode reachability, an ordinary pose, and no aftermath check — not History-mode specifically, an in-progress draft, or the dismiss-outcome button): (1) **History mode reachability** — with the encounter still active, switched the sidebar to History; the banner and the "Combat" nav button both stayed visible over History's own body, and clicking "Combat" routed back to Here/Room with `CombatRail` mounted (`aria-current="page"` moved to "Here"). (2) **Draft survival** — typed an unsent pose while in Conversations mode, clicked the banner (`jumpToCombat()`, sidebar → Here/Room, `CombatRail` visible), then switched back to Conversations: the exact typed string was still in the composer, unsent. (3) **Aftermath/dismiss-outcome** — ended the live encounter via the real `POST /api/combat/{id}/end/` REST action (see Tested interactions #10 for why a REST call was used instead of a raw DB write — a `SharedMemoryModel` idmapper-staleness gotcha empirically confirmed by first trying the direct-write approach and watching the frontend keep polling the completed row as still-active for 20+ seconds); the `EncounterOutcomeBanner` ("Encounter Concluded: Round 0", "Abandoned") rendered with its `data-testid="aftermath-dismiss"` button, clicking it fired `handleDismissOutcome`, and the banner/rail both cleared shortly after. Screenshots: `docs/reviews/assets/3761/a17-history-mode-reachable.png`, `docs/reviews/assets/3761/a17-draft-survives-jump.png`, `docs/reviews/assets/3761/a17-dismiss-outcome.png`. | |
| A18 | PASS | Resized the live page to 375x800; the mobile sidebar toggle opened a bottom-tab-driven panel with Here/Conversations/History all reachable, the room detail fully legible, and no page-level horizontal overflow. Screenshots: `docs/reviews/assets/3761/a18-mobile-375-viewport.png`, `docs/reviews/assets/3761/a18-mobile-sidebar-panel.png`. | 200% zoom and keyboard-only navigation were not independently exercised this pass. |
| A19 | PASS | (Fix round 1, live) Typed a real unsent draft, read its real `sessionStorage` entries (`arx:play-draft:v2:3:1:account:3:EvidenceWalkerFix1:room:2` and the legacy `arx:play-draft:v1:...` key), overwrote both with invalid JSON (`{not-valid-json::::garbage`), then navigated back into `/game`: the app reached "In world" normally with zero thrown JS errors (`page.on('pageerror')` recorded none) — no crash on corrupted storage. Logged out via the real header Logout control and logged into a second, distinct real account (`e2e_fix1_account2`/`SecondWalkerFix1`, its own character/tenure/sheet): that account's composer started completely empty, confirming no cross-account draft exposure even though account 1's raw `sessionStorage` entries were still present in the same browser tab (the draft key is scoped by account id, `arx:play-draft:v2:` followed by the accountId, so account 2's code never reads account 1's key). Screenshots: `docs/reviews/assets/3761/a19-corrupted-storage-safe-default.png`, `docs/reviews/assets/3761/a19-second-account-no-draft-leak.png`. | All three named results confirmed: no crash, no cross-account exposure, and safe handling. Correction to this row after further investigation (post fix-round-1): the `{not-valid-json::::garbage` string appearing verbatim in the composer is CORRECT behavior, not a defect. `CommandInput.tsx`'s `arx:play-draft:v1:*` key (the actual source of the visible textarea value) stores the raw draft text directly — it was never JSON-encoded, so there is no parse step to fail; echoing back whatever string was stored is exactly what a real player's own previously-typed text would do. The separate `arx:play-draft:v2:*` store (`useDraftStore.ts`) IS JSON-encoded and already has a correct try/catch → empty-draft fallback (`readStoredDraft`, `useDraftStore.ts:62-70`) that was never actually exercised as broken by this test — v2's `content` field is a one-way mirror of v1 for send-idempotency bookkeeping (`CommandInput.tsx`'s `handleChange` → `draftStore.setContent(val)`) and never feeds back into the textarea. Issue #3790 (filed for this apparent defect) was closed as not-a-bug after this correction. |
| A20 | PASS | Opened the sidebar's Conversations tab; the literal "OOC channels unavailable, pending #3299." note rendered, with only the real "Room" conversation entry listed — no fabricated channel list, no second backend, matching the umbrella issue's own stated alternate acceptance path for this gate ("unavailable rows are absent with the dependency documented"). This is Task 6's change on this branch, re-verified live. Screenshot: `docs/reviews/assets/3761/a20-channels-unavailable-note.png`. | |
| A21 | OUT_OF_SCOPE | n/a | Needs an injected old-page/search failure and a mid-fetch thread switch; not exercised live this pass (would need request interception) and unchanged by this branch's diff. |
| A22 | OUT_OF_SCOPE | n/a | Partition/migration schema behavior is explicitly scoped by the spec itself to CI Postgres parity, not a live UI journey ("do not run the full slow local parity suite or claim SQLite validates partition behavior"). This branch's diff contains zero migration files (`git diff --stat origin/main...HEAD -- src/world/migrations/` is empty), so A22 is untouched by Tasks 1-7. |

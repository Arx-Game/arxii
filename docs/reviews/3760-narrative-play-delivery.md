# Review evidence

- Reviewed revision: `687cebd502ad3900ce4e155d638a5666d046d869`
- Reviewer: `demo-fidelity-reviewer (agent)`
- Reviewer verdict: `FAIL`
- Application/build identity: Vite production build (`pnpm build` at the reviewed revision, `frontend/src/web/static/dist/`) served by Playwright's `vite preview --port 4173`, exercised via `page.routeWebSocket`/`page.route` fixture mocks (same pattern as the branch's own `frontend/e2e/narrative-play-delivery.spec.ts`) — not a live Django/Evennia backend.
- Environment: Linux devcontainer, Chromium 1208 (Playwright 1.58.2, headless), default 1280x720 viewport, light theme (no `.dark` class applied; theme was never toggled — see Unresolved findings, theme comparison is BLOCKED along with the rest of the visual comparison).
- Viewports/themes: 1280x720 / light only. No dark-theme or narrow-viewport capture was attempted, since a visual comparison against the demo could not be completed at all (see below) — capturing more permutations of an uncomparable render would not have added evidence.
- Approved design: https://claude.ai/code/artifact/9df13026-a448-4a47-b5a4-e4e011e79a53 ("Composer Delivery States") — **could not be reached.** See Unresolved findings for the exact attempts made.
- Visual review: **BLOCKED** — the application was rendered and screenshotted (see Screenshots below), but the approved-design reference could not be fetched by this reviewer, so no pixel/layout comparison against it was possible. This is not "looks fine from the template" — the five composer states were genuinely rendered in a real browser against the production bundle — but a screenshot with nothing to compare it to is not a visual review.
- Visual verdict: `BLOCKED`
- Screenshots: ![Screen 1 composing baseline](docs/reviews/screenshots/3760/1-composing-baseline.png) ![Screen 2 send pending](docs/reviews/screenshots/3760/2-send-pending.png) ![Screen 3a rejected](docs/reviews/screenshots/3760/3a-rejected.png) ![Screen 3b actually renders stranded, not unknown](docs/reviews/screenshots/3760/3b-unknown-outcome--actually-stranded.png) ![Screen 4 stranded draft](docs/reviews/screenshots/3760/4-stranded-draft.png) ![Screen 5 storage unavailable](docs/reviews/screenshots/3760/5-storage-unavailable.png)
- Comparison notes: No comparison against the demo was possible (BLOCKED — demo unreachable). What follows is a structural/behavioral review of the rendered application against the **issue spec's own text description** of each demo screen (quoted from `#3760`'s spec block, not from the image itself) plus two defects found independent of any demo access at all:
  1. **Demo Screen 3b ("Unknown outcome — Check status / Retry, never an automatic resend") is unreachable in the shipped build.** `CommandInput.tsx`'s reconnect-reconciliation effect (`frontend/src/game/components/CommandInput.tsx:439-461`) always nulls `pendingSpeechRef.current` (line 446) *before* it can ever call `markUnknown()` (line 459), and `composerBanner`'s derivation (`CommandInput.tsx:302-328`) treats any non-clean draft with no matching live `pendingSpeechRef` as `stranded`, not `unknown` — so the very next render after `markUnknown()` immediately resolves to the stranded banner instead. Confirmed two ways: (a) live Playwright interaction — drop the socket mid-send, reconnect, the composer goes straight from "Sending…" to "Unsent draft from…" and `send-unknown-banner` never mounts (verified with an explicit `toHaveCount(0)` assertion); (b) the branch's own `CommandInput.test.tsx:941-951` test, titled *"a hydrated unknown-status draft resolves to the stranded banner, not the live Check status/Retry pair,"* whose own comment states outright: *"there is currently no way to reach the live-unknown banner from CommandInput's own props/events... Task 12 should add its own CommandInput-level coverage once it can actually reach this banner"* — and Task 12 (the reconnect wiring, same file, lines 404-461) is already committed and, per (a), still does not reach it. The `send-unknown-banner` JSX, its copy, and its "Check status"/"Retry" buttons all exist and are correctly styled (Tailwind utility classes reach the page — no CSS-not-reaching-page defect here), but no code path in the shipped app can ever mount them for a real user. This is a **defect independent of demo access**: whatever Screen 3b shows, the built app cannot show its equivalent under any circumstance, because that state is dead.
  2. **The branch's own fixture-backed acceptance suite for this exact issue fails against the reviewed revision's production build**, for the two tests covering exactly the states this review is about. Run directly (`npx playwright test e2e/narrative-play-delivery.spec.ts`, unmodified, against the same freshly-built `dist/`):
     - `a dropped send retried with the same id lands exactly once` — **FAILS**: `stranded-draft-banner` never becomes visible after the mocked reconnect.
     - `a reconnect reauthorizes before flipping to ready` — **FAILS**: the Send button never re-enables.
     - The other two (`travel preserves separate drafts per room` / `per conversation tab`) pass.
     Root cause traced: `GameWindow.tsx`'s `playReady` (`GameWindow.tsx:272-276`) requires `effectiveLifecycle` to be `ready-scene`/`ready-no-scene`/`encounter`/`aftermath`; that transition only happens when a `room_state` frame arrives (`useGameSocket.ts:217-219`). The reconnect-open handler (`useGameSocket.ts:312-355`) re-puppets (`@ic <character>`) and reconciles drafts, but neither the app nor the mocked test WebSocket ever re-sends `room_state` on the *new* connection, so `effectiveLifecycle` stays stuck at `entering` ("Refreshing your confirmed location before play resumes…") forever in this fixture. I was able to make the reconnect journeys (and this review's own screenshots) succeed only by *manually* sending an extra `room_state` + `puppet_changed` pair on the reconnected socket — a workaround this review added, not something the committed spec or app does on its own. **I cannot tell from a fixture-only review whether this is (a) a gap in the committed test's mock (a real Evennia `@ic` re-puppet may genuinely re-broadcast `room_state`, in which case only the *test* needs fixing) or (b) a genuine ordering bug in the app's reconnect path (in which case real players would see the composer stuck disabled after every reconnect, at a cadence #3745 says is already roughly every 127 seconds in production).** This needs a human/implementer call, not a guess from this review — but as shipped, the issue's own acceptance evidence for its reconnect/retry/stranded-draft contract does not pass.
  3. **Demo Screen 7 ("An attached mechanical action, acknowledged independently of the prose") was not built.** The spec's own Design section commits to it explicitly: *"Action-attachment UI: a small acknowledged/pending badge on the composer (Screen 7 in the demo), sourced from the existing `ACTION_RESULT` bus (`useActionResult`) plus the new pending state until that arrives."* The diff (`git diff origin/main...HEAD --stat`) does not touch `frontend/src/scenes/components/ActionAttachment.tsx` or `ActionAttachment.test.tsx` at all, and that component (grepped in full) contains no "acknowledged," "pending," or badge concept of any kind, nor any `useActionResult` usage. `CommandInput.tsx` renders `<ActionAttachment ... />` unchanged from before this branch (`CommandInput.tsx:1022-1028`). There is no acknowledged/pending badge anywhere in the built surface.
  4. **Demo Screen 6 ("Reconnecting → Reconciling → Ready sequence") has no distinct visible UI counterpart.** The only reconnect-adjacent copy actually rendered is two generic status strings in `GameWindow.tsx:308-329`: *"Entering the world… waiting for a confirmed location. You can write while you wait."* / *"Connection lost. Your draft is safe; you can keep writing while we reconnect."* / *"Refreshing your confirmed location before play resumes…"* / *"Connection lost. Your confirmed story remains available while we reconnect."* — none of these say "Reconciling," and there is no 3-stage visible sequence; reconciliation (`reconcileStoredDrafts`, `useGameSocket.ts:336-355`) is silent, backend-logic-only from the UI's perspective. Spec Decision 6 frames this issue's job as *"Reconnect correctness, not reconnect cadence"* and the Design section describes reconnect as an internal *ordering* ("reauthorize → reconcile → flip Ready"), not a demo'd visible state machine — so this divergence may be intentional scope-narrowing rather than a defect, but I cannot confirm that against the actual demo image (BLOCKED), so I am not marking it PASS.
  5. Demo Screens 2/3a/4/5 (pending, rejected, stranded, storage-unavailable) all render with distinct, correctly-styled banners carrying the copy quoted in the Requirement ledger below, structurally matching the spec's one-line description of each screen. I cannot say more than that without the reference image.
- Tested interactions: Composing baseline load; open a whisper conversation tab; type + `Control+Enter` submit → pending banner + disabled textarea; simulated `action_result` rejection → rejected banner, textarea re-enabled, typed content preserved; simulated abnormal socket close (code 1011) mid-send → reconnect → (after this review's manual `room_state`/`puppet_changed` workaround) stranded-draft banner with working Discard/"Resume & retry" buttons visible in the DOM; forced `sessionStorage.setItem` to throw via `page.addInitScript` → storage-unavailable notice. Did **not** test: Check status/Retry buttons in a genuinely live (non-stranded) unknown state (confirmed unreachable, finding #1 above); the action-attachment badge (confirmed not built, finding #3); a literal reconnecting→reconciling→ready visible sequence (confirmed not built as distinct UI, finding #4); dark theme; any viewport other than 1280x720; REST-path (`submit_pose`) delivery states, only the WS `say`/`whisper` path; backend Python tests (`test_interaction_views.py`, `test_pose_submission_*.py`) were not run as part of this visual review — this review is scoped to the frontend composer surface per the dispatch.
- Fixture/live boundary: Everything in this review is fixture-backed. REST is mocked via `page.route('**/api/**', ...)` (account/roster/interactions/character-resonances/submissions endpoints only; everything else 404s). The game WebSocket is mocked via `page.routeWebSocket('**', ...)`, with this review manually sending `room_state`/`puppet_changed`/`interaction`/`action_result` frames to drive each state — no real Django/Evennia backend, no real database, and no real `PoseSubmission` ledger row was ever created or checked. The production JS/CSS bundle itself is real (built fresh from the reviewed revision via `pnpm build` immediately before this review, confirmed newer than the reviewed commit's timestamp).
- Overall outcome: `FAIL`

## Visual checklist

**Caveat, stated once and binding for every row below: I could not fetch the approved-design artifact** (see Unresolved findings for what was tried). Every "Expected" cell below is transcribed from the **issue's own spec text** describing each screen (the closest available proxy), not from the demo image itself — I have never seen the demo's actual pixels, layout, colors, or exact copy. No row below can honestly be marked `MATCH`; every row is `BLOCKED` pending a vision-capable reviewer who can actually open the artifact URL.

| Element | Expected | Result | Evidence |
|---|---|---|---|
| Screen 1 — Composing baseline | "the existing baseline (unchanged reader/composer chrome)" | BLOCKED | `docs/reviews/screenshots/3760/1-composing-baseline.png` |
| Screen 2 — Send pending | "composer disabled, spinner" | BLOCKED | `docs/reviews/screenshots/3760/2-send-pending.png` (spinner + "Sending…" + disabled textarea confirmed structurally present) |
| Screen 3a — Rejected | "typed reason inline, editing re-enabled immediately" | BLOCKED | `docs/reviews/screenshots/3760/3a-rejected.png` (inline reason text confirmed present; textarea confirmed re-enabled and editable) |
| Screen 3b — Unknown outcome | "Check status / Retry, never an automatic resend" | FAIL (not BLOCKED — see Comparison notes finding #1: this state is structurally unreachable, independent of any demo comparison) | `docs/reviews/screenshots/3760/3b-unknown-outcome--actually-stranded.png` shows what renders instead (the stranded banner) |
| Screen 4 — Stranded draft banner | "a stranded, unconfirmed draft offered for resume or discard" | BLOCKED | `docs/reviews/screenshots/3760/4-stranded-draft.png` (Discard + "Resume & retry" buttons confirmed present and wired) |
| Screen 5 — Storage unavailable | "explicit 'this tab only' notice" | BLOCKED | `docs/reviews/screenshots/3760/5-storage-unavailable.png` (notice text confirmed present) |
| Screen 6 — Reconnecting → Reconciling → Ready | a 3-stage visible sequence | FAIL (no distinct visible sequence exists at all — see Comparison notes finding #4) | No screenshot; confirmed absent by source read of `GameWindow.tsx:308-329` |
| Screen 7 — Action-attachment acknowledged badge | "acknowledged independently of the prose" | FAIL (not built — see Comparison notes finding #3) | No screenshot; confirmed absent by diff scope + full read of `ActionAttachment.tsx` |
| Screen 8 — Finalize table (what an accepted send writes) | admin/authoring reference table | OUT_OF_SCOPE | Spec explicitly disclaims this: "no admin authoring table applies; this issue has no authored content, so that section... is out of scope by design, not by omission" |

## Requirement ledger

| ID | Status | Evidence | Authorized decision |
|---|---|---|---|
| DEMO-1 | BLOCKED | Pending state renders (`docs/reviews/screenshots/3760/2-send-pending.png`, `CommandInput.tsx:907-915`, testid `send-pending-banner`, copy "Sending…"); demo reference unreachable so pixel/layout match against Screen 2 cannot be confirmed | |
| DEMO-2 | BLOCKED | Rejected state renders (`docs/reviews/screenshots/3760/3a-rejected.png`, `CommandInput.tsx:916-923`, testid `send-rejected-banner`, copy "Not sent — {rejectionReason}"); demo reference unreachable | |
| DEMO-3 | FAIL | Unknown-outcome banner (`CommandInput.tsx:924-946`, testid `send-unknown-banner`, copy "Connection dropped before we heard back. We don't know if this sent.", buttons "Check status"/"Retry") is unreachable by any live user path — see Comparison notes finding #1, confirmed by direct Playwright reproduction and by the branch's own `CommandInput.test.tsx:926-951` comment | |
| DEMO-4 | BLOCKED | Stranded-draft banner renders with working buttons (`docs/reviews/screenshots/3760/4-stranded-draft.png`, `CommandInput.tsx:884-906`, testid `stranded-draft-banner`, copy "Unsent draft from {context}. This never got a response last time.", buttons "Discard"/"Resume & retry"); demo reference unreachable. Reaching this state in the fixture required this review's own `room_state`/`puppet_changed` workaround — see Comparison notes finding #2 | |
| DEMO-5 | BLOCKED | Storage-unavailable notice renders (`docs/reviews/screenshots/3760/5-storage-unavailable.png`, `CommandInput.tsx:876-883`, testid `storage-unavailable-notice`, copy "Draft kept in this tab only — it won't survive a reload."); demo reference unreachable | |
| DEMO-6 | OUT_OF_SCOPE | No distinct visible Reconnecting→Reconciling→Ready UI sequence exists (`GameWindow.tsx:308-329` has two generic status strings only, no "Reconciling" state); reconciliation is silent/backend-only (`useGameSocket.ts:336-355`) | Spec Decision 6 frames this issue's job as reconnect *correctness*, not a visible cadence UI, and the Design section describes reconnect as an internal ordering rather than a demo'd state machine — plausible intentional scope-narrowing, but this reviewer could not confirm against the actual demo image (BLOCKED), so this is reported for human confirmation, not asserted as ratified |
| DEMO-7 | FAIL | No acknowledged/pending badge exists anywhere in `ActionAttachment.tsx` (untouched by this diff) or `CommandInput.tsx`'s use of it (`CommandInput.tsx:1022-1028`, unchanged); spec's own Design section commits to building this ("a small acknowledged/pending badge... Screen 7 in the demo") and it is absent | |
| COPY-FIDELITY | BLOCKED | Built banner copy quoted verbatim in Comparison notes and this ledger; cannot verify against demo's exact wording because the demo could not be fetched | |

## Unresolved findings

- **The approved-design artifact could not be reached.** Attempts made: (1) direct `curl` to `https://claude.ai/code/artifact/9df13026-a448-4a47-b5a4-e4e011e79a53` returns only the loading shell (the artifact frame is populated client-side via an authenticated `/api/frame/{uuid}` XHR after JS executes); (2) direct `curl` to that `/api/frame/...` endpoint returns a Cloudflare "Just a moment..." managed-challenge page (HTTP 403, JS+cookie challenge) rather than content; (3) no `Artifact` tool was available in this session's tool list (only `Bash` and `Read` were provided, unlike the `demo-fidelity-reviewer` agent definition's own stated `tools: Bash, Read, Grep, Glob`, which also does not list an `Artifact` tool despite the agent's own instructions directing its use — this looks like an environment/dispatch gap, not something this review can route around); (4) searched the issue body/comments (`gh api repos/Arx-Game/arxii/issues/3760`) for an attached image — none exists, only the artifact link as text; (5) searched the worktree for any previously-saved copy of the artifact (HTML, PNG, or otherwise) — none exists, only references to the same unreachable URL in `docs/superpowers/plans/2026-09-11-narrative-play-delivery.md`. **This review is therefore incomplete for its primary purpose** (comparing the rendered application against the approved reference) and must not be used to authorize opening the PR as-is. A human or a session with working artifact/browser access needs to complete the pixel-level comparison the Visual checklist above stubs out.
- **Finding #1 (Comparison notes): Demo Screen 3b's live "unknown outcome" state is unreachable in the shipped app.** See DEMO-3 above. Independent of demo access.
- **Finding #2 (Comparison notes): two of the branch's own four Playwright acceptance journeys (`frontend/e2e/narrative-play-delivery.spec.ts`) fail when run against the reviewed revision's actual production build**, and no CI workflow in this repo runs `pnpm test:e2e` at all (grepped `.github/workflows/`, no hits) — so nothing else will catch this. Needs a human call on whether the root cause is a test-fixture gap (mock doesn't resend `room_state` on reconnect, which a real Evennia `@ic` re-puppet may do) or a genuine app-level reconnect-readiness bug. Given #3745's ~127-second forced-reconnect cadence already in production, this is high-consequence either way.
- **Finding #3 (Comparison notes): Demo Screen 7's action-attachment acknowledged badge was not built**, despite the spec's own Design section committing to it explicitly. See DEMO-7 above.
- **Finding #4 (Comparison notes): Demo Screen 6's reconnect sequence has no distinct visible UI**, which may be intentional (per spec Decision 6) but could not be confirmed against the actual demo image. See DEMO-6 above.
- A pre-existing, unrelated local modification (`frontend/src/game/GamePage.test.tsx`, uncommitted, present in the worktree before this review began) was left untouched — not reviewed, not committed, not attributed to this issue.

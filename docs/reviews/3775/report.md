# Review evidence

Closes #3775.

- Reviewed revision: `88bb7b391cba5f88de0234e884ad996da569083f`
- Reviewer: demo-fidelity-reviewer (Sonnet 5 general-purpose subagent following tools/agents/demo-fidelity-reviewer.md)
- Reviewer verdict: PASS
- Application/build identity: admin screens rendered through the Django test client (a throwaway `world.codex.tests.test_zz_render_demo`, run with `just test-parity world.codex.tests.test_zz_render_demo` and deleted afterward) at the reviewed revision, then served with Django admin's own static CSS (`django/contrib/admin/static/admin`) symlinked alongside the saved HTML and screenshotted with the repo's own Playwright; the Codex page's staff line was rendered from a `pnpm build` production bundle (`src/web/static/dist`) served by `vite preview`, exercised by a throwaway `frontend/e2e/codex-staff-line.spec.ts` (also deleted afterward).
- Environment: devcontainer Linux 6.6.114.1-microsoft-standard-WSL2, Python 3.13 in the worktree's uv-managed venv, Postgres `test-parity` database, Node 20 / pnpm, Playwright Chromium (the repo's own install under `frontend/node_modules`).
- Viewports/themes: 1280x900, full-page screenshots, light theme (Django admin's default; no `dark_mode` toggle exercised). The Codex-page staff-line screenshots are 1280x900 viewport, not full-page.
- Approved design: https://claude.ai/artifact/GLUhLnH9fseka8KJt2PHko and issue #3775
- Visual review: Completed. Rendered and read by eye: the Codex entries changelist (default, and filtered to `reach=unreachable`), the "Secrets of the Five Castes" entry change form, the Profile change form for a character with two Beginnings rows, and the Codex page for a staff account and a non-staff account.
- Visual verdict: PASS
- Screenshots: ![Screen 1: Codex entries changelist, Is public and Known via columns (with perspective naming), By reach/By is public/By category/By subject filters](docs/reviews/3775/screen-1-changelist.png) ![Screen 1 filtered to Unreachable: 2 of 6 entries](docs/reviews/3775/screen-1b-unreachable.png) ![Screen 2: Secrets of the Five Castes change form, Who knows this leading, five grant inlines with Holders today, the read-only clue panel with placements, collapsed fieldsets](docs/reviews/3775/screen-2-entry-form.png) ![Screen 3: Profile change form, Beginnings inline with Peerage (where play began) and Twilight Court (a memory recovered, with note)](docs/reviews/3775/screen-3-profile-form.png) ![Screen 5: Codex page as staff, showing the staff-view line above the tree](docs/reviews/3775/screen-5-codex-staff-line.png) ![Screen 5b: Codex page as a player, no staff-view line](docs/reviews/3775/screen-5b-codex-player.png)
- Comparison notes: Screen 1 (`screen-1-changelist.png`) carries the Is public column as Django's standard boolean icon (green check / red X) where the demo drew plain "check"/"no" text; this is the idiomatic Django admin rendering of a `BooleanField` column and reads the same information, not a defect. The By reach filter (`All / Public / Granted to a group / Reached by a clue / Unreachable`) matches the demo verbatim, and filtering to Unreachable (`screen-1b-unreachable.png`) narrows to only the entries with no public flag, grant or clue, with a result count, matching the demo's Unreachable(64)-style narrowing (count differs only because the fixture seeds 6 entries, not the 119-row corpus the demo drew from). The Action dropdown carries all three new actions (`Publish (everyone can read)`, `Unpublish`, `Grant to current holders`) alongside the existing `Delete selected`. The Known via column now names the perspective holder: for "Secrets of the Five Castes" it reads "1 beginnings (perspective: The Blessed), 1 tradition, 1 organization, 1 clue", matching the demo's "1 beginnings (perspective: The Blessed)" and the spec's Design text ("naming the perspective holder when one exists"). Fixed in `ffa741089` (`CodexEntryAdmin.get_queryset`'s `beginnings_perspective`/`tradition_perspective` subqueries plus `known_via`'s per-part annotation, `src/world/codex/admin.py`).

  Screen 2 (`screen-2-entry-form.png`) leads with Who knows this (the Public checkbox and its help copy, matching the demo's wording almost verbatim), then the five grant inlines in the demo's order (Known from birth / Taught by a tradition / Taught on joining / Path / Distinction), each with a Holders today column reading "N characters" and an Is perspective checkbox where the demo shows one (checked for the Blessed row, matching the fixture). Costs and learning, Prerequisites, Mechanics link, Item pointer, Display order and Credit are all collapsed below, as the demo specifies. The Found through a Mystery clue panel now reads Slug and Placed, showing "catacomb-ledger | 1 room, 0 triggers" for the seeded `RoomClue` placement, matching the demo's "Clue | Placed" -> "catacomb-ledger | 1 room, 0 triggers" and the spec's Design text ("showing slug and placement count"). Fixed in `ffa741089`/`88bb7b391` (`ClueInline.placements`, summing `room_placements`/`trigger_placements` with natural pluralization, `src/world/codex/admin.py`). One structural note that is not a defect: the demo drew Path and Distinction sharing one fieldset box with two "+ Add another" links; the build renders them as two separate `TabularInline`s ("Path Codex Grants" and "Distinction Codex Grants"), which is the standard shape for two different models in Django admin (one fieldset per inline model) and matches the spec's "five grant inlines" wording; the demo's single box was a compactness choice for the mock, not a modeled requirement.

  Screen 3 (`screen-3-profile-form.png`) matches the demo closely: the Beginnings inline reads Beginnings / Source / Note / Gained at / Delete, and the two seeded rows reproduce the demo's own example almost verbatim (Peerage, "Where play began", no note; Twilight Court, "A memory recovered", note "Woke under the Catacombs, scene 412"). The inline's help copy ("The wizard adds where play began; add a recovered memory or past life here and its entries are granted on save") appears as the fieldset's header bar rather than as caption text below the add-row link as the demo draws it; the information is present, just relocated, so this is not counted as a defect. The demo's "Lineage" fieldset header around Heritage/Origin realm/Family is not drawn in the build because `ProfileAdmin` predates #3775 and has always used a flat `fields` tuple rather than named `fieldsets` (unchanged by this diff); the spec's own Design section only says the inline goes "under the Lineage fieldset" as a locational description, not a request to add fieldset styling, so this is pre-existing and out of this branch's scope, not a regression it introduced.

  Screens 5/5b (`screen-5-codex-staff-line.png`, `screen-5b-codex-player.png`, not one of the demo's four drawn screens but the "Player levers" table's Staff view row) match exactly: a staff account sees "Staff view: every entry is shown. Players see only what their characters know." above the tree, verbatim against both the demo table and the spec's Design section; a non-staff account sees no such line. These two screens are unchanged from the prior review pass (the fix commits touched only `src/world/codex/admin.py`).
- Tested interactions: navigated the changelist with and without `?reach=unreachable`; opened the entry change form for an entry carrying all five grant kinds plus a clue placed in a room; opened the Profile change form for a character with two Beginnings rows; loaded `/codex` with a mocked staff account and a mocked non-staff account and asserted the staff line's presence/absence. The three bulk actions (Publish, Unpublish, Grant to current holders) and the save-time grant-reach hooks were exercised by the named Python tests below rather than clicked through in this render pass (the render pass captures GET states only).
- Fixture/live boundary: the four admin screenshots use FactoryBoy fixtures created for this review (6 entries, not the demo's 119-row loaded corpus), so counts on screen 1/1b differ from the demo's numbers by construction (e.g. "3 beginnings" vs. the demo's "15 beginnings" for The Shroud) while the qualitative shape (which reach bucket, which filters, which actions, the perspective naming, the placement count) is the same data the real admin would show. This pass's fixture adds a `RoomClueFactory(clue=...)` placement so the clue panel's placement count has a real room to count (the prior pass's fixture had none, which would have rendered "0 rooms, 0 triggers" and masked whether the column was wired at all). The Codex-page staff-line screenshots (screen 5/5b) use Playwright route fixtures for `/api/user/`, `/api/roster/entries/mine/` and `/api/codex/categories/tree/` and never touch Django or Postgres; they isolate the one frontend behaviour (the staff line) from backend visibility, which is covered separately by `codex/tests/test_views.py::StaffSeesEverythingTests`.
- Overall outcome: PASS

## Requirement ledger

| id | status | evidence | authorized decision |
| --- | --- | --- | --- |
| entry-list-public-and-known-via-columns | PASS | `codex/tests/test_admin.py::test_changelist_shows_public_and_known_via`; `screen-1-changelist.png` | |
| unreachable-and-reach-filters | PASS | `codex/tests/test_admin.py::test_reach_filter_unreachable`, `test_reach_filter_buckets`; `screen-1b-unreachable.png` | |
| publish-unpublish-actions | PASS | `codex/tests/test_admin.py::test_publish_flips_rows_and_skips_clue_targets`, `test_unpublish_clears_featured`; Action dropdown in `screen-1-changelist.png` | |
| entry-form-leads-with-who-knows-this | PASS | `codex/tests/test_admin.py::test_change_form_renders_grant_inlines`; `screen-2-entry-form.png` | |
| grant-row-reaches-existing-holders | PASS | `codex/tests/test_admin.py::test_saving_a_new_grant_inline_reaches_existing_holders`; `codex/tests/test_grant_to_current_holders.py` (`BeginningsHoldersTests`, `TraditionHoldersTests`, `PathHoldersTests`, `DistinctionHoldersTests`, `SpeciesHoldersTests`) | |
| grant-to-current-holders-action | PASS | `codex/tests/test_admin.py::test_grant_to_holders_creates_knowledge`; `OrganizationGrantAdminTests::test_creating_a_grant_reaches_current_members` | |
| known-via-names-the-perspective-holder | PASS | `src/world/codex/admin.py` (`get_queryset`'s `beginnings_perspective`/`tradition_perspective` subqueries, `known_via`'s per-part annotation, commit `ffa741089`); `screen-1-changelist.png` shows "1 beginnings (perspective: The Blessed), 1 tradition, 1 organization, 1 clue" for Secrets of the Five Castes | |
| clue-panel-shows-placement-count | PASS | `src/world/codex/admin.py` (`ClueInline.placements`, commits `ffa741089`/`88bb7b391`); `screen-2-entry-form.png` shows "SLUG / PLACED" -> "catacomb-ledger / 1 room, 0 triggers" | |
| profile-beginnings-set-two-origins | PASS | `character_sheets/tests/test_profile_beginnings.py::test_sheet_reads_the_true_profile_set`, `test_one_where_play_began_per_profile`, `test_same_beginnings_twice_is_refused`; `character_sheets/tests/test_profile_admin_beginnings.py::test_adding_a_recovered_origin_grants_its_entries`; `screen-3-profile-form.png` | |
| clue-and-public-flag-refuse-each-other | PASS | `codex/tests/test_models.py` (`CodexEntry.clean` clue guard, `models.py:325-335`); `clues/tests/test_models.py::ClueTargetsPublicEntryTests::test_public_codex_target_refused` | |
| staff-reader-sees-everything | PASS | `codex/tests/test_views.py::StaffSeesEverythingTests::test_staff_lists_hidden_entry_with_content`, `test_player_without_the_flag_does_not`; `screen-5-codex-staff-line.png`, `screen-5b-codex-player.png` | |
| finalize-writes-profilebeginnings | PASS | `character_creation/tests/test_finalize_beginnings.py::test_writes_one_character_creation_row`, `test_idempotent`, `test_no_beginnings_writes_nothing` | |
| migration-is-schema-only | PASS | `src/world/migrations/0140_profilebeginnings.py` is a single `CreateModel` (no `RunPython`); `max_migration.txt` updated to `0140_profilebeginnings`, dependency on `0139_worshippedbeing_gm_notes_organizationcodexgrant` | |
| docs-updated-in-tandem | PASS | `docs/systems/codex.md`, `docs/systems/INDEX.md`, `docs/systems/MODEL_MAP.md`, `docs/systems/character_creation.md`, `docs/systems/character_sheets.md`, `docs/roadmap/codex.md`, `src/world/codex/AGENT_GLOSSARY.md`, `docs/adr/README.md` + the new ADR file all touched in the diff | |
| p1-global-tier-entries-become-public | OUT_OF_SCOPE | no code change; the Publish action exists for staff to run it | spec Scope: staff admin pass after deploy |
| production-catch-up | OUT_OF_SCOPE | Grant to current holders and the Beginnings inline exist for staff to run this by hand | spec Scope: staff admin pass after deploy |

## Visual checklist

| Element | Expected | Result | Evidence |
| --- | --- | --- | --- |
| Changelist: Is public column | A boolean indicator per row (demo draws check/no text; Django's built-in green-check/red-X icon for a `BooleanField` column carries the same information) | MATCH | `screen-1-changelist.png` |
| Changelist: Known via column, counts | "N beginnings", "N tradition", etc. summed across the five grant tables plus clues, e.g. "1 tradition" for The Sanguinus | MATCH | `screen-1-changelist.png` |
| Changelist: Known via column, perspective naming | Names the perspective holder when one exists, e.g. "1 beginnings (perspective: The Blessed)" | MATCH | `screen-1-changelist.png` shows "1 beginnings (perspective: The Blessed), 1 tradition, 1 organization, 1 clue" for Secrets of the Five Castes |
| Changelist: By reach filter | All / Public / Granted to a group / Reached by a clue / Unreachable | MATCH | `screen-1-changelist.png` |
| Changelist: By public filter | All / Yes / No (demo labels it "By public"; Django's default `BooleanField` filter title renders "By is public", same three options) | MATCH | `screen-1-changelist.png` |
| Changelist: category/subject filters | Existing By category, By subject filters retained | MATCH | `screen-1-changelist.png` |
| Changelist: three actions | Publish (everyone can read), Unpublish, Grant to current holders in the Action dropdown | MATCH | `screen-1-changelist.png` (Action select options) |
| Changelist: Unreachable filter narrows the list | Selecting Unreachable shows only entries with no public flag, no grant, no clue, with a result count | MATCH | `screen-1b-unreachable.png` |
| Entry form: Who knows this leads | Public checkbox with help text about paths, ahead of Costs and learning | MATCH | `screen-2-entry-form.png` |
| Entry form: five grant inlines | Beginnings / Tradition / Organization / Path / Distinction grant tables, each with a Holders today column | MATCH | `screen-2-entry-form.png` |
| Entry form: Is perspective checkbox | Reflects `is_perspective` per row (checked for the Blessed's Beginnings grant) | MATCH | `screen-2-entry-form.png` |
| Entry form: clue panel columns | Slug and a placement count (demo: "Clue / Placed" -> "catacomb-ledger / 1 room, 0 triggers") | MATCH | `screen-2-entry-form.png` shows "SLUG / PLACED" -> "catacomb-ledger / 1 room, 0 triggers" |
| Entry form: collapsed fieldsets | Costs and learning, Prerequisites, Mechanics link, Item pointer, Display order, Credit all collapsed below Who knows this | MATCH | `screen-2-entry-form.png` |
| Profile form: Beginnings inline columns | Beginnings, Why (source), Note, Gained, Delete | MATCH | `screen-3-profile-form.png` |
| Profile form: two origin rows | Peerage / Where play began (no note); Twilight Court / A memory recovered / "Woke under the Catacombs, scene 412" | MATCH | `screen-3-profile-form.png` |
| Profile form: add-row and help copy | "+ Add another..." row plus explanatory copy about the wizard and staff additions | MATCH | `screen-3-profile-form.png` (copy relocated to the fieldset header bar; same information) |
| Codex page: staff-view line | "Staff view: every entry is shown. Players see only what their characters know." shown above the tree for a staff account only | MATCH | `screen-5-codex-staff-line.png`, `screen-5b-codex-player.png` |

## Unresolved findings

None

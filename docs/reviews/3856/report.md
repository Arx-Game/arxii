# Review evidence: web client look output, command feedback, one feed (#3856, PR 1 of 3)

Refs #3856 (the issue stays open for PR 2, filter chips, and PR 3, the Actions fold).

- Reviewed revision: faa4a44e8db52bdcd53b04467e8f9680ae842510
- Reviewer: Claude Fable 5.1 (implementing session), vision review of the rendered application screenshots below, plus a separate demo-fidelity review pass (a general-purpose Sonnet 5 subagent following `tools/agents/demo-fidelity-reviewer.md`, since the named agent is not registered in this session) that read the approved demo's HTML and the same four screenshots; its checklist and findings are folded into the tables below.
- Reviewer verdict: PASS
- Application/build identity: `pnpm build` of this branch at faa4a44e8 (Vite production bundle, output `src/web/static/dist`), served by `vite preview --port 4173` exactly as `playwright.config.ts` does.
- Environment: devcontainer (Ubuntu 24.04 x64), Chrome Headless Shell 145.0.7632.6 via Playwright 1.58.2 (chromium v1208), headless, `hasTouch: true` per `playwright.config.ts`.
- Viewports/themes: 1280x800, light theme (the system default; no `data-theme` stamp) for three screenshots and the `dark` class for the fourth.
- Approved design: issue #3856's spec, treatment A "One feed, filter chips" (demo v5, https://claude.ai/code/artifact/d802b376-8d16-4d5a-9c49-5b3c3890b5f8, approved by Dan on 2026-09-14). PR 1's scope from the spec's PR plan: typed text frames from the server, a note per text frame in both readers (look with subject and prose body, error in the danger tokens, item and system boxed, arrive and move as italic lines, ambience as the italic hairline line), and the "you" row in the room list. Chips, the chip editor, minimise and dismiss, wake, the Actions fold and the entrance star are PR 2 and PR 3 and are not reviewed here.
- Visual review: completed on the four screenshots below, taken from the running application (not a mockup) by `frontend/e2e/feed-notes.spec.ts`, compared against the demo's `.note`, `.note.err`, `.mv` and `.amb` rendering and its room list.
- Visual verdict: PASS
- Screenshots: ![No scene, 1280 wide, light: look, error, item, arrival, ambience and departure notes in the column; the Here panel lists Tehom YOU first](https://raw.githubusercontent.com/Arx-Game/arxii/faa4a44e8db52bdcd53b04467e8f9680ae842510/docs/reviews/3856/notes-exploration-1280.png) ![Scene, Threads view, 1280 wide, light: a look note and an error note between two poses, an arrival line after](https://raw.githubusercontent.com/Arx-Game/arxii/faa4a44e8db52bdcd53b04467e8f9680ae842510/docs/reviews/3856/notes-scene-threads-1280.png) ![Scene, Chronological view, 1280 wide, light: the same column flat](https://raw.githubusercontent.com/Arx-Game/arxii/faa4a44e8db52bdcd53b04467e8f9680ae842510/docs/reviews/3856/notes-scene-chronological-1280.png) ![Scene, Chronological view, 1280 wide, dark: the error note in red-400 on the dark ground](https://raw.githubusercontent.com/Arx-Game/arxii/faa4a44e8db52bdcd53b04467e8f9680ae842510/docs/reviews/3856/notes-scene-dark-1280.png)
- Comparison notes: In `notes-exploration-1280.png` the column below the room description holds, in order, a boxed look note (◎ glyph, "Quiet courtyard" title line, the appearance in the serif reading face), the error note in the danger tokens with a `!` glyph and Evennia's own wording including the double-quoted suggestion, a boxed item note with a ✦ glyph, an italic muted arrival line, the ambience line in italics behind a left hairline with the gemit tag rendered green from Evennia's colour span, an italic departure line, and a second boxed look note for the "look me" reply. The demo draws exactly these forms: a boxed muted-sans note with a narrow glyph column, the error variant recoloured with the danger tokens, bare italic lines for arrivals and departures, and the ambience line with a hairline. No "System" strip renders anywhere. The Here panel's Characters section reads "CHARACTERS (2)" and lists "Tehom YOU" above "Nyx", the demo's room-list order and tag. In `notes-scene-threads-1280.png` the look note and the error note sit between Nyx's first and second standalone poses and the arrival line follows the second, matching the frames' order in time; `notes-scene-chronological-1280.png` shows the same column after switching views. `notes-scene-dark-1280.png` shows the error note's text and border in red-400 on the dark ground, legible; the first build used the palette's deep destructive red there and was corrected before this revision. Differences from the demo that are not defects: the demo's look note takes its title from a separate subject field, and the server does not send one yet, so the title here is the first line of the appearance text, which is the target's name; the demo's note glyph column is 14px wide and the build's is 14px (`w-3.5`); the demo places notes at the column's left edge in the scene view and the build indents them by the reader's pose gutter (`px-2`) so they align with the pose cards. The reference (history) view gets no notes by design. The demo-fidelity pass found two cosmetic drifts at the previous revision adc82467a (the look note's title was semibold where the demo keeps it plain, and the "you" tag was a filled chip where the demo writes plain uppercase muted text); both were fixed in the reviewed revision and the screenshots retaken, and the pass found no other divergence.
- Tested interactions: open `/game` and reach "In world" (room_state and puppet_changed over the mocked socket) with and without an active scene; type `look` and press Enter (the socket receives `look`); receive a `text` frame typed `look` and see the note; type `lok` and press Enter (the socket receives `lok`); receive the no-match line typed `error` and see it as an alert; receive `item`, `arrive`, `narrative` and `move` frames and assert the six notes' kinds in order; assert no "System" text renders; press the "Tehom you" row in the Here panel (the socket receives `look me`) and see the reply as a look note; in a scene, receive two poses and three typed frames and assert the Threads column order (pose, note, note, pose, note) and the note kinds; switch to Chronological and assert the same kinds; add the dark class and screenshot. Zero page errors in both journeys.
- Fixture/live boundary: REST (`/api/user/`, `/api/roster/entries/mine/`, `/api/interactions/`, `/api/magic/character-resonances/`) and the game WebSocket are mocked with `page.route` and `page.routeWebSocket`, following `game-entry.spec.ts` and `narrative-play-delivery.spec.ts`; the text frames are sent in the shape the server produces (Evennia's tuple form lands as the frame's kwargs, and the Portal's colour conversion lands as HTML spans). No Django or Evennia process and no database took part in the screenshots. The server side of the contract (`feed_kind`, `send_result`, `CmdNoMatch`, the `arrive` retype, the narrative service's tuple form) is pinned by the Python tests named in the ledger, run on the SQLite tier. The rendered bundle, both readers, `FeedNoteBlock`, `CharactersList` and the CSS are the real ones; only the backend is absent.
- Overall outcome: PASS

## Requirement ledger

| id | status | evidence | authorized decision |
| --- | --- | --- | --- |
| server-types-look-item-error | PASS | `commands.tests.test_feed_kind` (11 tests): a kinded command sends `(text, {"type": kind})`, a failed result and a `CommandError` are typed `error`, the item verbs and `CmdLook` carry their kinds | n/a |
| server-no-match-typed-error | PASS | `commands.tests.test_feed_kind.CmdNoMatchTests` (4 tests): Evennia's wording with suggestions, typed `error`; `CmdNoMatch` registered in `CharacterCmdSet` | n/a |
| server-arrival-typed-arrive | PASS | `typeclasses.tests.test_move_announcements` (2 tests): the new room hears `arrive`, the old room `move`, the stealth echo `arrive` | n/a |
| server-narrative-types-reach-wire | PASS | `world.narrative.tests.test_services` asserts the tuple form `(text, {"type": "narrative"})`; `broadcast_gemit` sends `text=(formatted, {"type": "gemit"})` | n/a |
| client-classifies-every-text-frame | PASS | `feedKinds.test.ts` (18 cases) and `useGameSocket.test.ts` (5 routing cases): every `text` frame becomes `addFeedNote` with the kind from `kwargs.type`, `narrative`/`gemit` as `ambience`, untyped as `system` | n/a |
| client-notes-store | PASS | `gameSlice.test.ts` notes block (9 tests): ids, subject, unread, bound of 200, survives a room change; `clearFeedNotes` | n/a |
| notes-in-no-scene-reader | PASS | `ExplorationReader.test.tsx`: one Activity list ordered by time (note, interaction, note); `feed-notes.spec.ts` journey 1; `notes-exploration-1280.png` | n/a |
| notes-in-scene-reader-both-views | PASS | `ThreadedNarrativeReader.test.tsx` notes block (3 tests): Threads order, Chronological order, a note alone shows instead of the empty prompt; `feed-notes.spec.ts` journey 2; `notes-scene-threads-1280.png`, `notes-scene-chronological-1280.png` | n/a |
| note-styling-per-demo | PASS | `FeedNoteBlock.test.tsx` (9 tests) and the visual checklist below; `notes-exploration-1280.png`, `notes-scene-dark-1280.png` | n/a |
| system-lane-removed | PASS | `SystemLane.tsx` and its test deleted; `feed-notes.spec.ts` asserts no "System" text renders; `GameWindow.tsx` renders no strip | n/a |
| you-row-in-room-list | PASS | `CharactersList.test.tsx` (4 tests) and `FocusPanel.test.tsx`: viewer listed first, tagged "you", counted, click sends `look me`; `feed-notes.spec.ts` journey 1; `notes-exploration-1280.png` | `look me` instead of `look <name>` (spec wording): `me` can never prefix-match another occupant; recorded in the PR body for Dan |
| sort-by-parsed-time | PASS | `feedRows.test.ts` (4 tests): server timestamps without milliseconds sort correctly against client ISO timestamps, stable on ties, unparsable last | n/a |
| docs-in-tandem | PASS | `frontend/src/game/CLAUDE.md`, `frontend/src/hooks/CLAUDE.md`, `frontend/src/store/CLAUDE.md`, `src/commands/CLAUDE.md`, `src/typeclasses/CLAUDE.md`, `docs/systems/scenes.md`, `docs/systems/narrative.md`, `docs/roadmap/rp-scenes.md` updated in this branch | n/a |
| chips-editor-minimise-dismiss-wake | OUT_OF_SCOPE | PR 2 of the same issue; not built here by the spec's PR plan | spec: "three PRs" |
| actions-fold | OUT_OF_SCOPE | PR 3 of the same issue | spec: "three PRs" |

## Visual checklist

| Element | Expected | Result | Evidence |
| --- | --- | --- | --- |
| Look note box | Muted sans box with a left rule, ◎ glyph in a narrow column, title line, description in the serif reading face | MATCH | `notes-exploration-1280.png`: two look notes, "Quiet courtyard" and "Tehom" titles, serif bodies |
| Error note | The same box recoloured with the danger tokens, `!` glyph, Evennia's wording with the double-quoted suggestion | MATCH | `notes-exploration-1280.png`, `notes-scene-threads-1280.png`: red text, red left rule, pale red fill |
| Error note, dark theme | Legible red on the dark ground | MATCH | `notes-scene-dark-1280.png`: red-400 text and rule |
| Item note | Boxed note with a ✦ glyph, one muted line | MATCH | `notes-exploration-1280.png`: "You pick up a lantern." |
| Arrival and departure lines | Bare italic muted lines, no box | MATCH | `notes-exploration-1280.png`: "Nyx arrives from the west.", "Nyx leaves, heading east."; `notes-scene-*`: "Corvin arrives from the east." |
| Ambience line | Italic muted line behind a left hairline, colour markup kept | MATCH | `notes-exploration-1280.png`: green "[GEMIT]" tag then the line, hairline on the left |
| Notes among poses, Threads | Notes at their time between the pose cards | MATCH | `notes-scene-threads-1280.png`: look and error between Nyx's two poses, arrival after |
| Notes among poses, Chronological | The same column flat | MATCH | `notes-scene-chronological-1280.png` |
| No System strip | Nothing collapsed below the feed | MATCH | all four: no "System" control anywhere |
| Room list "you" row | Viewer first, avatar initials, name, a small "you" tag, counted in the heading | MATCH | `notes-exploration-1280.png`: "CHARACTERS (2)", "Tehom YOU", then "Nyx" |
| Composer and Here panel intact | Toolbar with bold, italic, strike, link and one colour button; the Here panel's nine-tab grid | MATCH | all four: toolbar and grid unchanged |

## Unresolved findings

None

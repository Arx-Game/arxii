# Review evidence: the Atlas and the staff link action can make a one-way exit, and unfiled rooms are reachable from the rail (#3860)

Closes #3860.

- Reviewed revision: `559717518598b68bb0515c1702e37418228e8016`
- Reviewer: Claude Fable 5.1 (implementing session), vision review of the rendered application screenshots below, plus a demo-fidelity review pass (a general-purpose Sonnet 5 subagent following `tools/agents/demo-fidelity-reviewer.md`, since the named agent is not registered in this session) that read the approved demo's HTML, the same five screenshots, the diff against main and every named test; it returned FAIL on one finding, folded in before the PR opened: the screen 3 frame had been taken at the start of the direction buttons' 150 ms colour transition (Exit back already gone, the fills not yet swapped, so Both ways still read as pressed). The journey now reads the pressed and resting fills off the page and waits for them to reach One way (and back to Both ways) before each frame; the screenshots below are the refreshed ones, re-checked by eye. Its other notes were pre-existing or pre-ruled divergences, recorded below.
- Reviewer verdict: PASS
- Application/build identity: `pnpm build` of this branch at the reviewed revision (Vite production bundle, output `src/web/static/dist`), served by `vite preview --port 4173` exactly as `playwright.config.ts` does.
- Environment: devcontainer (Ubuntu 24.04 x64), Chrome Headless Shell 145.0.7632.6 via Playwright 1.58.2 (chromium v1208), headless, `hasTouch: true` per `playwright.config.ts`.
- Viewports/themes: 1280x900, light theme (the system default; no `data-theme` stamp).
- Approved design: issue #3860's spec (demo v1, https://claude.ai/code/artifact/1be34ba7-aa5d-4480-be7a-95a0a1b0bad3, built from Dan's design direction of 2026-09-14, which carries `spec:approved`). Five screens: Limbo opened from Unfiled rooms; the exit dialog with Both ways pressed; One way pressed with Exit back gone; the one-way link landed with its tagged chip and toast; the two-way link unchanged.
- Visual review: completed on the five screenshots below, taken from the running application (not a mockup) by `frontend/e2e/atlas-one-way.spec.ts`, compared against the demo's rail (`#unfiled`, "Unfiled rooms — 2", Limbo and Sandbox), its document (the Exits marginalia, the no-area line), its dialog (Leads to, the `#dir` pair, Exit there, `#backcol`, `#note`) and its landed state (the chip's `.ow` tag, the toast copy).
- Visual verdict: PASS
- Screenshots: ![Screen 1: the rail's Unfiled rooms section lists Limbo and Sandbox; Limbo's document is open with "this room has no area to place it within" and an empty Exits marginalia](https://raw.githubusercontent.com/Arx-Game/arxii/559717518598b68bb0515c1702e37418228e8016/docs/reviews/3860/screen-1-unfiled-1280.png) ![Screen 2: the exit dialog names City Center; Both ways is pressed, Exit there and Exit back are both shown, the note reads "links to City Center, and City Center back to here"](https://raw.githubusercontent.com/Arx-Game/arxii/559717518598b68bb0515c1702e37418228e8016/docs/reviews/3860/screen-2-both-ways-1280.png) ![Screen 3: One way is pressed, Exit back is gone, the note reads "links to City Center; nothing leads back to here"](https://raw.githubusercontent.com/Arx-Game/arxii/559717518598b68bb0515c1702e37418228e8016/docs/reviews/3860/screen-3-one-way-1280.png) ![Screen 4: after Link it, the Exits marginalia shows the "down" chip with its ONE WAY tag and the toast reads "Linked Limbo -> City Center (one way)."](https://raw.githubusercontent.com/Arx-Game/arxii/559717518598b68bb0515c1702e37418228e8016/docs/reviews/3860/screen-4-linked-one-way-1280.png) ![Screen 5: a two-way link lands as before, the "down" chip carries no tag and the toast reads "Linked Limbo &lt;-&gt; City Center."](https://raw.githubusercontent.com/Arx-Game/arxii/559717518598b68bb0515c1702e37418228e8016/docs/reviews/3860/screen-5-linked-both-ways-1280.png)
- Comparison notes: In `screen-1-unfiled-1280.png` the rail carries "UNFILED ROOMS — 2" in the same small-caps plate head as Pinned and Recent, with Limbo and Sandbox as rows under the area tree, and Limbo's document is open with the compass line "this room has no area to place it within" and Exits at 0, the demo's screen 1. In `screen-2-both-ways-1280.png` the dialog reads Leads to, the "⇢ link to City Center" suggestion, the Both ways / One way pair with Both ways pressed (filled) and One way outlined, Exit there, Exit back and the italic note "links to City Center, and City Center back to here", the demo's screen 2. In `screen-3-one-way-1280.png` One way is pressed, Exit back is gone and the note reads "links to City Center; nothing leads back to here", the demo's screen 3. In `screen-4-linked-one-way-1280.png` the Exits marginalia counts 1 and the "down" chip carries the small-caps "ONE WAY" tag in the accent colour; the toast at the bottom right reads "Linked Limbo -> City Center (one way).", the demo's screen 4. In `screen-5-linked-both-ways-1280.png` the chip has no tag and the toast reads "Linked Limbo &lt;-&gt; City Center.", the demo's screen 5. Differences from the demo that are not defects: the demo's note names the current room ("nothing leads back to Limbo") while the dialog, which does not know the room it is opened from, says "back to here"; the demo's chip shows the destination ("down → City Center") while the app's existing chip shows the exit name only (the destination was never on the chip before this issue, and the chip opens the exit editor which names it); the demo's rail is a sketch of the real Folio rail, which also has Pinned, Recent and the tree; the app's dialog has a close control and the suggestion row it already had.
- Tested interactions: open `/staff/world-builder` as staff; read "Unfiled rooms — 2" with Limbo and Sandbox; press Limbo and read its document, the no-area compass line and an empty exit band; press "⊕ dig or link an exit…", type "City" and take the suggestion; read Both ways pressed, Exit back present and the both-ways note; press One way and read Exit back gone and the one-way note; press Both ways and read Exit back return; name the exit "down" and Link it one way, then read the dispatched kwargs (`room_a_id`, `room_b_id`, `name_ab: 'down'`, `one_way: true`, no `name_ba`), the toast and the tagged chip; the same both ways with "up" and read `name_ba: 'up'`, no `one_way`, the untagged chip and the two-way toast; open the page as a non-staff account and read no Unfiled section (the staff route sends them home). Zero page errors asserted on screen 1.
- Fixture/live boundary: the account, roster and builder REST endpoints (`areas/`, the area's `manager/`, `grants/`, `unfiled-rooms/`, `room-detail/`, `room-search/`) and the action dispatch are Playwright route fixtures in the journey, answering as the server does (the dispatch fixture grows the detail's exits so the invalidated document refetches them); no Django or Evennia process and no database took part, so the server side (`create_exit`, `one_way_exit_ids`, `StaffLinkRoomsAction` `one_way`, the `one_way` rows, `unfiled-rooms`) is covered by the Python tests named in the ledger rather than the journey. The rail, the document, the dialog, the chips, the toast and the CSS are the real ones.
- Overall outcome: PASS

## Requirement ledger

| id | status | evidence | authorized decision |
| --- | --- | --- | --- |
| staff-link-action-takes-one-way | PASS | `test_one_way_link_makes_only_the_forward_exit` (one exit, message "Linked A -> B (one way).", no `name_ba`); `test_two_way_link_still_needs_both_names` | spec Decision 1 |
| one-direction-minting-is-public | PASS | `test_create_exit_mints_one_direction_only`; `create_exit_pair` unchanged | spec Design |
| both-ways-is-the-default | PASS | `AddDialog.test.tsx` "one way hides the exit back..." (Both ways `aria-pressed` on open); journey screen 2; `screen-2-both-ways-1280.png` | spec Decision 1 |
| one-way-omits-the-return-and-says-so | PASS | `AddDialog.test.tsx` (Exit back gone, note "nothing leads back"); `RoomDocument.test.tsx` "a one-way link sends one_way and no return name"; journey screens 3, 4; `screen-3-one-way-1280.png` | spec Decision 1 |
| unfiled-rooms-in-the-rail | PASS | `test_unfiled_rooms_lists_area_less_rooms_for_staff_only`; `IndexRail.test.tsx` "lists unfiled rooms for staff and opens one as its document"; journey screen 1; `screen-1-unfiled-1280.png` | spec Decision 2 |
| unfiled-listing-is-staff-only-and-hidden-when-empty | PASS | `test_unfiled_rooms_lists_area_less_rooms_for_staff_only` (a warrant holder gets `[]`); `IndexRail.test.tsx` "shows no unfiled section when there are none, and never to a non-staff warrant"; journey non-staff case | spec Decision 2 |
| only-staff-mint-one-way-exits | PASS | `LinkRoomsAction`/`StoryLinkRoomsAction` untouched (still `create_exit_pair`); ADR-0301 | spec Decision 5 (proposed, easy to flip) |
| exit-rows-carry-one-way | PASS | `test_exit_rows_say_whether_anything_leads_back` (manager); `test_room_detail_exits_say_whether_anything_leads_back` (detail); `test_one_way_exit_ids_knows_a_pair_from_a_lone_exit` (one query) | spec Decision 6 |
| room-document-marks-a-one-way-exit | PASS | `Marginalia.test.tsx` "tags a one-way exit on its chip"; `RoomDetailPanel.tsx` " · one way"; journey screen 4; `screen-4-linked-one-way-1280.png` | spec Decision 6 |
| unlink-copes-with-a-dangling-exit | PASS | `test_unlinks_a_dangling_one_way_exit_without_crashing` (pre-existing, green) | spec Decision 6 |
| canvas-link-dialog-gets-the-pair | PASS | `LinkRoomsDialog.tsx` direction pair, hidden `nameBA`, `one_way` kwarg | spec Decision 7 |
| leads-to-already-reaches-unfiled-rooms | PASS | `room_search` unchanged (not area-scoped for staff); journey's suggestion row | spec Decision 7 |
| two-way-link-unchanged | PASS | journey screen 5 (`name_ab`, `name_ba`, no `one_way`; "Linked Limbo &lt;-&gt; City Center."); `screen-5-linked-both-ways-1280.png` | spec screen 5 |
| docs-in-tandem | PASS | `docs/systems/areas.md` "One-way exits and unfiled rooms"; `docs/systems/INDEX.md` Areas; ADR-0301 + index | n/a |
| what-limbo-is | OUT_OF_SCOPE | ruling: staff's call, no code | spec Decision 3 |

## Visual checklist

| Element | Expected | Result | Evidence |
| --- | --- | --- | --- |
| Rail section | "Unfiled rooms — 2" in the rail's plate-head style, under the tree | MATCH | `screen-1-unfiled-1280.png` |
| Rail rows | Limbo, then Sandbox; pressing one opens its document | MATCH | `screen-1-unfiled-1280.png`, journey |
| Limbo's document | Name, "this room has no area to place it within", Exits empty with "⊕ dig or link an exit…" | MATCH | `screen-1-unfiled-1280.png` |
| Dialog: Leads to | Input with the destination, the search suggestion beneath | MATCH | `screen-2-both-ways-1280.png` |
| Dialog: direction pair | Both ways pressed by default, One way beside it, same two-button style as the dialog's rows | MATCH | `screen-2-both-ways-1280.png` |
| Dialog: Exit there / Exit back | Both shown when Both ways | MATCH | `screen-2-both-ways-1280.png` |
| Dialog: note (both ways) | "links to City Center, and City Center back to here" (the demo says "to Limbo"; the dialog does not know the room's name) | MATCH | `screen-2-both-ways-1280.png` |
| Dialog: One way pressed | One way carries the pressed fill, Both ways the outline (asserted on the computed background colour) | MATCH | `screen-3-one-way-1280.png` |
| Dialog: Exit back on One way | Hidden | MATCH | `screen-3-one-way-1280.png` |
| Dialog: note (one way) | "links to City Center; nothing leads back to here" (the demo says "to Limbo") | MATCH | `screen-3-one-way-1280.png` |
| Toast (one way) | "Linked Limbo -> City Center (one way)." | MATCH | `screen-4-linked-one-way-1280.png` |
| Exit chip (one way) | "down" with the small-caps ONE WAY tag (the demo also prints the destination, which the chip never carried; the exit editor names it) | MATCH | `screen-4-linked-one-way-1280.png` |
| Toast (both ways) | "Linked Limbo &lt;-&gt; City Center." | MATCH | `screen-5-linked-both-ways-1280.png` |
| Exit chip (both ways) | "down" with no tag | MATCH | `screen-5-linked-both-ways-1280.png` |
| Rest of the page | Folio rail, crumb, document and marginalia unchanged | MATCH | `screen-1-unfiled-1280.png`, `screen-5-linked-both-ways-1280.png` |

## Unresolved findings

None

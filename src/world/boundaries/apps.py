from django.apps import AppConfig


class BoundariesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "world.boundaries"
    verbose_name = "Player Content Boundaries"

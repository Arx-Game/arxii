# Generated by Django 5.2.14 on 2026-05-23 14:28

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("missions", "0018_missionnode_editor_x_missionnode_editor_y"),
    ]

    operations = [
        migrations.AddField(
            model_name="missionnode",
            name="flavor_text",
            field=models.TextField(
                blank=True,
                help_text="Thin abstract description of the moment shown to the player when they enter this node (design §8.2). The rich narration is the player-authored Legend Entry; this is just the engine's framing line.",
            ),
        ),
        migrations.AddField(
            model_name="missionnode",
            name="flavor_text_needs_rewrite",
            field=models.BooleanField(
                default=False,
                help_text="Phase-D copy operation sets True (inherited copy reads as 'rewrite me'); editing flavor_text clears it. Surfaces in the Studio's 'N flavor fields are still flagged as un-rewritten copy' counter (design §10).",
            ),
        ),
        migrations.AddField(
            model_name="missionoption",
            name="authored_ic_framing_needs_rewrite",
            field=models.BooleanField(
                default=False,
                help_text="Phase-D copy operation sets True (inherited copy reads as 'rewrite me'); editing authored_ic_framing clears it. Surfaces in the Studio's 'N flavor fields are still flagged as un-rewritten copy' counter (design §10).",
            ),
        ),
        migrations.AddField(
            model_name="missionoptionroute",
            name="outcome_text",
            field=models.TextField(
                blank=True,
                help_text="Player-facing outcome text shown when this route's tier is rolled (design §8.3). STORED BUT UNCONSUMED in Phase B — the resolution engine doesn't surface outcome_text today; Phase D wires it into the player message.",
            ),
        ),
        migrations.AddField(
            model_name="missionoptionroute",
            name="outcome_text_needs_rewrite",
            field=models.BooleanField(
                default=False,
                help_text="Phase-D copy operation sets True (inherited copy reads as 'rewrite me'); editing outcome_text clears it.",
            ),
        ),
        migrations.AddField(
            model_name="missionoptionroutecandidate",
            name="outcome_text_needs_rewrite",
            field=models.BooleanField(
                default=False,
                help_text="Phase-D copy operation sets True (inherited copy reads as 'rewrite me'); editing outcome_text clears it.",
            ),
        ),
    ]

# Generated by Django 5.2.14 on 2026-05-23 05:24

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):
    dependencies = [
        ("missions", "0015_missiongiverstanding_delete_missiongivercooldown_and_more"),
        ("objects", "0013_defaultobject_alter_objectdb_id_defaultcharacter_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="missioncategory",
            name="display_order",
            field=models.PositiveIntegerField(
                default=0,
                help_text="Browse ordering in the authoring tool (lower = earlier). No Meta.ordering on the model — callers order explicitly via ``order_by('display_order', 'name')``.",
            ),
        ),
        migrations.AlterField(
            model_name="missiongiver",
            name="environmental_detail",
            field=models.ForeignKey(
                blank=True,
                help_text="ENVIRONMENTAL_DETAIL kind: the examinable thing in the room (real item or room detail) that kicks off the offer. Must be null for other kinds. Should be an Item-typeclass ObjectDB or a room-detail ObjectDB.",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="+",
                to="objects.objectdb",
            ),
        ),
        migrations.AlterField(
            model_name="missiongiver",
            name="location",
            field=models.ForeignKey(
                blank=True,
                help_text="Optional Evennia room/location anchoring this giver. For ROOM_TRIGGER kind, this room IS the trigger (entering it rolls the offer). Should be a Room-typeclass ObjectDB.",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="+",
                to="objects.objectdb",
            ),
        ),
        migrations.AlterField(
            model_name="missiongiver",
            name="npc",
            field=models.ForeignKey(
                blank=True,
                help_text="NPC kind: the abstract giver NPC the player talks to (not piloted, not a sheet). Must be null for other kinds. Should be a Character-typeclass ObjectDB (or a giver-NPC subclass — staff convention not enforced at the model layer).",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="+",
                to="objects.objectdb",
            ),
        ),
        migrations.AlterField(
            model_name="missiongiveroffering",
            name="requirements_override",
            field=models.JSONField(
                blank=True,
                default=dict,
                help_text="Optional per-offering predicate gate (Phase-0 tree shape). STORED BUT UNCONSUMED in Phase B — services.availability reads only the template's availability_rule today; Phase D wires this override in (semantic: AND-compose with the template rule). Empty {} = no per-offering override.",
            ),
        ),
        migrations.AlterField(
            model_name="missiongiveroffering",
            name="weight_override",
            field=models.PositiveIntegerField(
                blank=True,
                help_text="Optional per-offering draw weight; null = use template.base_weight. Must be >= 1 when set — 0 would silently disable this offering, which is not the right tool (use the template's is_active flag or delete the offering instead).",
                null=True,
            ),
        ),
        migrations.AlterField(
            model_name="missiongiverstanding",
            name="affection",
            field=models.IntegerField(
                default=0,
                help_text="Per-character standing / affection with this giver. Authoring tool exposes 'giver_standing_at_least' predicate gates against this value (Phase C). Negative values are permitted and mean disliked — the Phase-C 'giver_standing_at_least: N' gate uses plain >= comparison so it works uniformly across the integer range (e.g. 'at least -5' is True for affection=-3, False for affection=-10). Movement mechanic (flirt/seduce checks against the NPC) is adjacent gameplay work, not built here.",
            ),
        ),
    ]

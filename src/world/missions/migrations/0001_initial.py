# Generated by Django 5.2.14 on 2026-05-29 17:28

import datetime

import django.core.validators
from django.db import migrations, models
import django.db.models.deletion

import core.natural_keys


class Migration(migrations.Migration):
    initial = True

    dependencies = [
        ("objects", "0013_defaultobject_alter_objectdb_id_defaultcharacter_and_more"),
    ]

    operations = [
        migrations.CreateModel(
            name="MissionCategory",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("name", models.CharField(max_length=100, unique=True)),
                ("description", models.TextField(blank=True)),
                (
                    "display_order",
                    models.PositiveIntegerField(
                        default=0,
                        help_text="Browse ordering in the authoring tool (lower = earlier). No Meta.ordering on the model — callers order explicitly via ``order_by('display_order', 'name')``.",
                    ),
                ),
            ],
            options={
                "verbose_name_plural": "Mission categories",
            },
            bases=(core.natural_keys.NaturalKeyMixin, models.Model),
        ),
        migrations.CreateModel(
            name="MissionDeedRewardLine",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "kind",
                    models.CharField(
                        choices=[
                            ("immediate", "Immediate"),
                            ("post_cron", "Post Cron"),
                            ("propagation", "Propagation"),
                        ],
                        max_length=12,
                    ),
                ),
                (
                    "sink",
                    models.CharField(
                        choices=[
                            ("money", "Money"),
                            ("legend_points", "Legend Points"),
                            ("resonance", "Resonance"),
                            ("rumor", "Rumor"),
                            ("crime_watch", "Crime Watch"),
                            ("beat", "Beat"),
                        ],
                        max_length=14,
                    ),
                ),
                (
                    "amount",
                    models.PositiveIntegerField(
                        blank=True,
                        help_text="Numeric magnitude of the reward, when applicable.",
                        null=True,
                    ),
                ),
                (
                    "ref",
                    models.CharField(
                        blank=True,
                        help_text="Optional reference/discriminator (e.g., a rumor key).",
                        max_length=200,
                    ),
                ),
            ],
            options={
                "abstract": False,
            },
        ),
        migrations.CreateModel(
            name="MissionGiver",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("name", models.CharField(max_length=200, unique=True)),
                (
                    "giver_kind",
                    models.CharField(
                        choices=[
                            ("npc", "NPC"),
                            ("environmental_detail", "Environmental Detail"),
                            ("room_trigger", "Room Trigger"),
                        ],
                        default="room_trigger",
                        help_text="How this giver reaches the player; selects the target's expected typeclass.",
                        max_length=20,
                    ),
                ),
                ("is_active", models.BooleanField(default=True)),
            ],
            options={
                "abstract": False,
            },
        ),
        migrations.CreateModel(
            name="MissionGiverOffering",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "weight_override",
                    models.PositiveIntegerField(
                        blank=True,
                        help_text="Optional per-offering draw weight; null = use template.base_weight. Must be >= 1 when set — 0 would silently disable this offering, which is not the right tool (use the template's is_active flag or delete the offering instead).",
                        null=True,
                    ),
                ),
                (
                    "requirements_override",
                    models.JSONField(
                        blank=True,
                        default=dict,
                        help_text="Optional per-offering predicate gate (Phase-0 tree shape). STORED BUT UNCONSUMED in Phase B — services.availability reads only the template's availability_rule today; Phase D wires this override in (semantic: AND-compose with the template rule). Empty {} = no per-offering override.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionGiverStanding",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("available_at", models.DateTimeField()),
                (
                    "affection",
                    models.IntegerField(
                        default=0,
                        help_text="Per-character standing / affection with this giver. Authoring tool exposes 'giver_standing_at_least' predicate gates against this value (Phase C). Negative values are permitted and mean disliked — the Phase-C 'giver_standing_at_least: N' gate uses plain >= comparison so it works uniformly across the integer range (e.g. 'at least -5' is True for affection=-3, False for affection=-10). Movement mechanic (flirt/seduce checks against the NPC) is adjacent gameplay work, not built here.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionInstance",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "status",
                    models.CharField(
                        choices=[
                            ("active", "Active"),
                            ("complete", "Complete"),
                            ("abandoned", "Abandoned"),
                            ("expired", "Expired"),
                        ],
                        default="active",
                        max_length=10,
                    ),
                ),
                ("started_at", models.DateTimeField(auto_now_add=True)),
                ("completed_at", models.DateTimeField(blank=True, null=True)),
            ],
            options={
                "abstract": False,
            },
        ),
        migrations.CreateModel(
            name="MissionNode",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "key",
                    models.SlugField(
                        help_text="Stable per-template node key (unique within the template).",
                        max_length=100,
                    ),
                ),
                ("is_entry", models.BooleanField(default=False)),
                (
                    "conflict_mode",
                    models.CharField(
                        choices=[
                            ("coinflip", "Coin Flip"),
                            ("vote", "Vote"),
                            ("joint", "Joint"),
                        ],
                        help_text="How contested option choices resolve for multiple participants.",
                        max_length=10,
                    ),
                ),
                (
                    "joint_combine",
                    models.CharField(
                        blank=True,
                        choices=[("any", "Any"), ("all", "All"), ("count", "Count")],
                        help_text="JOINT mode: how participant results combine.",
                        max_length=10,
                        null=True,
                    ),
                ),
                (
                    "joint_count",
                    models.PositiveSmallIntegerField(
                        blank=True,
                        help_text="JOINT+COUNT mode: minimum number of successes required.",
                        null=True,
                    ),
                ),
                (
                    "deny_all_riders",
                    models.BooleanField(
                        default=False,
                        help_text="When true, no consequence riders may attach at this node. NOT consumed by the engine in Phase A; reserved for future use.",
                    ),
                ),
                (
                    "editor_x",
                    models.IntegerField(
                        default=0,
                        help_text="Mission Studio (Phase E) canvas X coordinate. Pure authoring metadata — no engine meaning. IntegerField (negatives allowed) so authors can pan to negative coords.",
                    ),
                ),
                (
                    "editor_y",
                    models.IntegerField(
                        default=0,
                        help_text="Mission Studio (Phase E) canvas Y coordinate. Pure authoring metadata — no engine meaning. IntegerField (negatives allowed) so authors can pan to negative coords.",
                    ),
                ),
                (
                    "flavor_text",
                    models.TextField(
                        blank=True,
                        help_text="Thin abstract description of the moment shown to the player when they enter this node (design §8.2). Paragraph-style (TextField, unbounded) — the short per-option label is the option's authored_ic_framing (CharField/200). The rich narration is the player-authored Legend Entry; this is just the engine's framing line.",
                    ),
                ),
                (
                    "flavor_text_needs_rewrite",
                    models.BooleanField(
                        default=False,
                        help_text="Phase-D copy service sets True (inherited copy reads as 'rewrite me'); the Phase-D edit service clears it on save. Surfaces in the Studio's 'N flavor fields are still flagged as un-rewritten copy' counter (design §10). NOT cleared automatically at the model layer — service responsibility.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionNodeSnapshot",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("taken_at", models.DateTimeField(auto_now_add=True)),
            ],
            options={
                "abstract": False,
            },
        ),
        migrations.CreateModel(
            name="MissionOption",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "order",
                    models.PositiveSmallIntegerField(
                        help_text="Display/evaluation order within the node (no Meta.ordering — callers order explicitly)."
                    ),
                ),
                (
                    "option_kind",
                    models.CharField(
                        choices=[("branch", "Branch"), ("check", "Check")],
                        max_length=10,
                    ),
                ),
                (
                    "source_kind",
                    models.CharField(
                        choices=[("authored", "Authored"), ("challenge", "Challenge")],
                        max_length=10,
                    ),
                ),
                (
                    "visibility_rule",
                    models.JSONField(
                        blank=True,
                        default=dict,
                        help_text="Phase 0 predicate tree gating this option's visibility.",
                    ),
                ),
                ("authored_base_risk", models.PositiveSmallIntegerField(default=0)),
                (
                    "authored_ic_framing",
                    models.CharField(
                        blank=True,
                        help_text="Short option-row label shown in the choice list (CharField/200). Keep terse; the longer 'what happens at this node' narration belongs in MissionNode.flavor_text.",
                        max_length=200,
                    ),
                ),
                (
                    "authored_ic_framing_needs_rewrite",
                    models.BooleanField(
                        default=False,
                        help_text="Phase-D copy service sets True (inherited copy reads as 'rewrite me'); the Phase-D edit service clears it on save. Surfaces in the Studio's 'N flavor fields are still flagged as un-rewritten copy' counter (design §10). NOT cleared automatically at the model layer — service responsibility.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionOptionRoute",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "is_random_set",
                    models.BooleanField(
                        default=False,
                        help_text="When true, destination is drawn from weighted candidates.",
                    ),
                ),
                (
                    "outcome_text",
                    models.TextField(
                        blank=True,
                        help_text="Player-facing outcome text shown when this route's tier is rolled (design §8.3). STORED BUT UNCONSUMED in Phase B — the resolution engine doesn't surface outcome_text today; Phase D wires it into the player message.",
                    ),
                ),
                (
                    "outcome_text_needs_rewrite",
                    models.BooleanField(
                        default=False,
                        help_text="Phase-D copy service sets True; the Phase-D edit service clears it on save. NOT cleared automatically at the model layer — service responsibility.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionOptionRouteCandidate",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("weight", models.PositiveSmallIntegerField(default=1)),
                (
                    "outcome_text",
                    models.TextField(
                        blank=True,
                        help_text="Optional per-candidate outcome text shown to the player. STORED BUT UNCONSUMED in Phase B — Phase D wires it.",
                    ),
                ),
                (
                    "outcome_text_needs_rewrite",
                    models.BooleanField(
                        default=False,
                        help_text="Phase-D copy service sets True; the Phase-D edit service clears it on save. NOT cleared automatically at the model layer — service responsibility.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionOptionRouteReward",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "kind",
                    models.CharField(
                        choices=[
                            ("immediate", "Immediate"),
                            ("post_cron", "Post Cron"),
                            ("propagation", "Propagation"),
                        ],
                        help_text="When the emitted line pays out (IMMEDIATE/POST_CRON/PROPAGATION).",
                        max_length=12,
                    ),
                ),
                (
                    "sink",
                    models.CharField(
                        choices=[
                            ("money", "Money"),
                            ("legend_points", "Legend Points"),
                            ("resonance", "Resonance"),
                            ("rumor", "Rumor"),
                            ("crime_watch", "Crime Watch"),
                            ("beat", "Beat"),
                        ],
                        help_text="Which ledger the emitted line pays into.",
                        max_length=14,
                    ),
                ),
                (
                    "amount",
                    models.PositiveIntegerField(
                        blank=True,
                        help_text="Numeric magnitude of the broadcast reward, when applicable.",
                        null=True,
                    ),
                ),
                (
                    "ref",
                    models.CharField(
                        blank=True,
                        help_text="Optional reference/discriminator (e.g., a rumor key).",
                        max_length=200,
                    ),
                ),
                (
                    "contract_holder_only",
                    models.BooleanField(
                        default=False,
                        help_text="True → emit exactly one line to the instance's contract holder, regardless of how many participants ran the mission. False → distribute per the template's reward_group_rule.",
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionParticipant",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("is_contract_holder", models.BooleanField(default=False)),
            ],
        ),
        migrations.CreateModel(
            name="MissionRewardQueue",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "kind",
                    models.CharField(
                        choices=[
                            ("immediate", "Immediate"),
                            ("post_cron", "Post Cron"),
                            ("propagation", "Propagation"),
                        ],
                        help_text="Mirrors the line's kind so the cron filters cheaply without an extra join.",
                        max_length=12,
                    ),
                ),
                (
                    "sink",
                    models.CharField(
                        choices=[
                            ("money", "Money"),
                            ("legend_points", "Legend Points"),
                            ("resonance", "Resonance"),
                            ("rumor", "Rumor"),
                            ("crime_watch", "Crime Watch"),
                            ("beat", "Beat"),
                        ],
                        help_text="Mirrors the line's sink so the cron filters cheaply without an extra join.",
                        max_length=14,
                    ),
                ),
                ("applied", models.BooleanField(default=False)),
                ("applied_at", models.DateTimeField(blank=True, null=True)),
                (
                    "failure_reason",
                    models.CharField(
                        blank=True,
                        help_text="Populated when applied=True but the cron's sink call raised; cron telemetry — not used in Phase 5b.1.",
                        max_length=500,
                    ),
                ),
            ],
        ),
        migrations.CreateModel(
            name="MissionTemplate",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("name", models.CharField(max_length=200, unique=True)),
                (
                    "summary",
                    models.TextField(help_text="Rich IC opening lore (mission bookend)."),
                ),
                (
                    "epilogue",
                    models.TextField(blank=True, help_text="Rich IC wrap-up lore."),
                ),
                ("level_band_min", models.PositiveSmallIntegerField()),
                ("level_band_max", models.PositiveSmallIntegerField()),
                ("risk_tier", models.PositiveSmallIntegerField()),
                (
                    "base_weight",
                    models.PositiveIntegerField(
                        default=1, help_text="Relative weight in the availability draw."
                    ),
                ),
                (
                    "arc_scope",
                    models.CharField(
                        choices=[
                            ("global", "Global"),
                            ("org", "Organization"),
                            ("giver", "Giver"),
                        ],
                        help_text="Whether this is offered globally, per-org, or per-giver.",
                        max_length=10,
                    ),
                ),
                (
                    "percent_replace",
                    models.PositiveSmallIntegerField(
                        default=0,
                        help_text="Percent chance this template replaces an existing offer (0-100).",
                    ),
                ),
                (
                    "cooldown",
                    models.DurationField(
                        help_text="Per-giver re-offer cooldown. Must be non-negative.",
                        validators=[
                            django.core.validators.MinValueValidator(datetime.timedelta(0))
                        ],
                    ),
                ),
                (
                    "reward_group_rule",
                    models.CharField(
                        choices=[
                            ("all_equal", "All Equal"),
                            ("by_role", "By Role"),
                            ("by_participation", "By Participation"),
                        ],
                        default="all_equal",
                        help_text="Multi-participant payout split (authoring knob only; actual distribution-by-rule is Phase 5).",
                        max_length=16,
                    ),
                ),
                (
                    "availability_rule",
                    models.JSONField(
                        blank=True,
                        default=dict,
                        help_text="Phase 0 predicate tree gating front-door availability for this template.",
                    ),
                ),
                ("is_active", models.BooleanField(default=True)),
                (
                    "access_tier",
                    models.CharField(
                        choices=[("open", "Open"), ("staff_only", "Staff Only")],
                        db_index=True,
                        default="staff_only",
                        help_text="Audience gate: STAFF_ONLY hides the template from all but is_staff_observer characters (the 'in testing' state — the production-safe default for new templates). OPEN lets the usual predicate / cooldown / level-band filters take over. Phase B-7 intentionally ships only two tiers; richer tiers (society, GM-level, etc.) follow after a permission-design brainstorm.",
                        max_length=16,
                    ),
                ),
            ],
            options={
                "abstract": False,
            },
        ),
        migrations.CreateModel(
            name="MissionDeedRecord",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("applied_at", models.DateTimeField(auto_now_add=True)),
                (
                    "actor",
                    models.ForeignKey(
                        help_text="The acting participant's character — consequence follows the actor.",
                        on_delete=django.db.models.deletion.PROTECT,
                        related_name="+",
                        to="objects.objectdb",
                    ),
                ),
            ],
            options={
                "abstract": False,
            },
        ),
    ]

# Generated by Django 5.2.15 on 2026-07-19 01:03

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("checks", "0024_consequenceeffect_asset_status_target"),
        ("evennia_extensions", "0012_playerdata_looking_for_table"),
        ("items", "0038_materialcategory_and_more"),
        ("magic", "0112_alter_technique_name"),
        ("mechanics", "0011_propertydetonation"),
        ("missions", "0033_missionoption_required_act_and_more"),
        ("npc_services", "0024_alter_npcserviceoffer_kind"),
        ("societies", "0031_domaincrisistype_domaincrisis_chosen_at_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="missionoption",
            name="key",
            field=models.SlugField(
                default="",
                help_text="Stable per-node authoring key (unique within the node), independent of order — reordering options for display must never change their identity. Required for real authored content; the '' default only exists so this schema migration doesn't need a data backfill.",
                max_length=100,
            ),
        ),
        migrations.AddField(
            model_name="missionoptionroutereward",
            name="sequence",
            field=models.PositiveSmallIntegerField(
                default=0,
                help_text="NK discriminator only — reward lines have no display order (nothing ever reorders them). 0 is the 'auto-assign' sentinel: save() computes the next value per parent (route or candidate) when left at 0. Pass an explicit non-zero value only for fixture round-trip / deliberate override — see save().",
            ),
        ),
        migrations.AddField(
            model_name="missionrenownaward",
            name="sequence",
            field=models.PositiveSmallIntegerField(
                default=0,
                help_text="NK discriminator only — same rationale as MissionOptionRouteReward.sequence. 0 is the auto-assign sentinel.",
            ),
        ),
        migrations.AddConstraint(
            model_name="missionoption",
            constraint=models.UniqueConstraint(
                fields=("node", "key"), name="unique_missionoption_node_key"
            ),
        ),
        migrations.AddConstraint(
            model_name="missionoptionroutecandidate",
            constraint=models.UniqueConstraint(
                fields=("route", "target_node"),
                name="unique_missionoptionroutecandidate_route_target",
            ),
        ),
        migrations.AddConstraint(
            model_name="missionoptionroutereward",
            constraint=models.UniqueConstraint(
                condition=models.Q(("route__isnull", False)),
                fields=("route", "sequence"),
                name="unique_reward_route_sequence",
            ),
        ),
        migrations.AddConstraint(
            model_name="missionoptionroutereward",
            constraint=models.UniqueConstraint(
                condition=models.Q(("candidate__isnull", False)),
                fields=("candidate", "sequence"),
                name="unique_reward_candidate_sequence",
            ),
        ),
        migrations.AddConstraint(
            model_name="missionrenownaward",
            constraint=models.UniqueConstraint(
                fields=("route", "sequence"), name="unique_renownaward_route_sequence"
            ),
        ),
    ]

# Generated by Django 5.2.14 on 2026-06-05 03:37

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):
    dependencies = [
        ("missions", "0007_missioninstance_accepted_as_persona"),
        ("societies", "0006_philosophicalarchetype"),
    ]

    operations = [
        migrations.CreateModel(
            name="MissionRenownAward",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "magnitude",
                    models.CharField(
                        blank=True,
                        choices=[
                            ("small", "Small"),
                            ("moderate", "Moderate"),
                            ("high", "High"),
                            ("very_high", "Very High"),
                        ],
                        help_text="Drives fame buffer + prestige-from-deeds. Blank = no fame/prestige delta (e.g., a Risk-only secret-deed award).",
                        max_length=16,
                    ),
                ),
                (
                    "risk",
                    models.CharField(
                        blank=True,
                        choices=[
                            ("none", "None"),
                            ("low", "Low"),
                            ("moderate", "Moderate"),
                            ("high", "High"),
                            ("extreme", "Extreme"),
                        ],
                        default="none",
                        help_text="Drives legend. NONE / blank = no legend awarded — e.g., a royal wedding has high Magnitude and None Risk.",
                        max_length=16,
                    ),
                ),
                (
                    "reach_override",
                    models.CharField(
                        blank=True,
                        choices=[
                            ("local", "Local"),
                            ("regional", "Regional"),
                            ("continental", "Continental"),
                            ("world", "World"),
                        ],
                        help_text="Reach override. Blank = derive from Magnitude per MAGNITUDE_TO_DEFAULT_REACH (Small→LOCAL, Moderate→REGIONAL, High→CONTINENTAL, Very High→WORLD).",
                        max_length=16,
                    ),
                ),
                (
                    "contract_holder_only",
                    models.BooleanField(
                        default=True,
                        help_text="True (default) → renown bundle awards only to the contract holder persona. False → broadcast per the template's reward_group_rule, same semantics as MissionOptionRouteReward.",
                    ),
                ),
                (
                    "archetypes",
                    models.ManyToManyField(
                        blank=True,
                        help_text="Philosophical archetype tags. Their principle deltas sum, then dot-product against each affected society's principle values to produce that society's reputation delta. Blank = no reputation deltas fire (Magnitude-only or Risk-only event).",
                        related_name="mission_awards",
                        to="societies.philosophicalarchetype",
                    ),
                ),
                (
                    "candidate",
                    models.ForeignKey(
                        blank=True,
                        help_text="Parent candidate (per-candidate award bundle). Stored but unconsumed until candidate-emit lands (parallels the MissionOptionRouteReward Phase D wiring on the flat-reward side). Exactly one of route / candidate must be set.",
                        null=True,
                        on_delete=django.db.models.deletion.CASCADE,
                        related_name="renown_awards",
                        to="missions.missionoptionroutecandidate",
                    ),
                ),
                (
                    "route",
                    models.ForeignKey(
                        blank=True,
                        help_text="Parent route (route-level award). Exactly one of route / candidate must be set; enforced in clean().",
                        null=True,
                        on_delete=django.db.models.deletion.CASCADE,
                        related_name="renown_awards",
                        to="missions.missionoptionroute",
                    ),
                ),
            ],
            options={
                "constraints": [
                    models.CheckConstraint(
                        condition=models.Q(
                            models.Q(("route__isnull", False), ("candidate__isnull", True)),
                            models.Q(("route__isnull", True), ("candidate__isnull", False)),
                            _connector="OR",
                        ),
                        name="missionrenownaward_exactly_one_parent",
                    )
                ],
            },
        ),
    ]

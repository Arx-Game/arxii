# Generated by Django 5.2.16 on 2026-09-04 23:06

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("arxii", "0220_upbringings"),
    ]

    operations = [
        migrations.AddField(
            model_name="technique",
            name="hit_narration",
            field=models.TextField(
                blank=True,
                default="",
                help_text="Player-facing line for when this technique lands on a target (#3554). Use {actor} and {target}; both are required. The machine appends the damage figure and any wound, knockout or defeat clause after it, so write the blow, not the ledger: '{actor} hurls a spear of rime at {target}'. Blank falls back to the standard sentence. Plain register.",
            ),
        ),
        migrations.AddField(
            model_name="technique",
            name="miss_narration",
            field=models.TextField(
                blank=True,
                default="",
                help_text="Player-facing line for when this technique misses its target (#3554). Use {actor} and {target}; both are required. Blank falls back to the standard sentence. Plain register.",
            ),
        ),
        migrations.AddField(
            model_name="threatpoolentry",
            name="hit_narration",
            field=models.TextField(
                blank=True,
                default="",
                help_text="Player-facing line for when this NPC attack lands on a target (#3554). Use {actor} and {target}; both are required. The machine appends the damage figure and any wound, knockout or defeat clause after it, so write the blow, not the ledger: '{actor} rakes {target} with its claws'. Blank falls back to the standard sentence. Plain register.",
            ),
        ),
        migrations.AddField(
            model_name="threatpoolentry",
            name="miss_narration",
            field=models.TextField(
                blank=True,
                default="",
                help_text="Player-facing line for when this NPC attack misses its target (#3554). Use {actor} and {target}; both are required. Blank falls back to the standard sentence. Plain register.",
            ),
        ),
    ]

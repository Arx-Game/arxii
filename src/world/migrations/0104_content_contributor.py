# Generated by Django 5.2.15 on 2026-08-04 22:51

from django.db import migrations, models
import django.db.models.deletion

import core.natural_keys


class Migration(migrations.Migration):
    dependencies = [
        ("arxii", "0103_giftunlock_unique_gift_unlock_per_gift"),
    ]

    operations = [
        migrations.CreateModel(
            name="ContentContributor",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "name",
                    models.CharField(
                        help_text="Credited name. Free of an account on purpose - an outside writer or a commissioned artist may never have one.",
                        max_length=100,
                        unique=True,
                    ),
                ),
                (
                    "notes",
                    models.TextField(
                        blank=True,
                        default="",
                        help_text="Staff-only context: contact, commission terms, what this person worked on.",
                    ),
                ),
            ],
            options={
                "ordering": ["name"],
            },
            bases=(core.natural_keys.NaturalKeyMixin, models.Model),
        ),
        migrations.AddField(
            model_name="playerdata",
            name="contributor",
            field=models.OneToOneField(
                blank=True,
                help_text="Content-credit identity for this account, if they author content. The link lives here rather than on ContentContributor so the exported corpus never carries a username (ADR-0010, ADR-0196).",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="player_data",
                to="arxii.contentcontributor",
            ),
        ),
    ]

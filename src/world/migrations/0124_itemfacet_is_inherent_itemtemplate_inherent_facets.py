# Generated by Django 5.2.16 on 2026-09-11 19:20

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("arxii", "0123_beingfacet"),
    ]

    operations = [
        migrations.AddField(
            model_name="itemfacet",
            name="is_inherent",
            field=models.BooleanField(
                default=False,
                help_text="True when this facet came from the template's inherent_facets at creation time, not a crafter's own attach_facet_to_item call. Does not count against the instance's facet_capacity.",
            ),
        ),
        migrations.AddField(
            model_name="itemtemplate",
            name="inherent_facets",
            field=models.ManyToManyField(
                blank=True,
                help_text="Facets this archetype always carries (the 'IS the thing' case — a Scythe template always carries the Scythe facet). Auto-stamped onto every instance at creation (see ItemInstance.save()); does not consume that instance's own facet_capacity.",
                related_name="inherent_on_templates",
                to="arxii.facet",
            ),
        ),
    ]

# Generated by Django 5.2.15 on 2026-07-26 02:51

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):
    dependencies = [
        ("checks", "0027_alter_checktypecapabilitymodifier_weight"),
        ("conditions", "0026_conditioncheckmodifier_check_category"),
    ]

    operations = [
        migrations.AddField(
            model_name="conditioninstance",
            name="is_aware",
            field=models.BooleanField(
                default=True,
                help_text="Whether the bearer is IC-aware of this condition. Unaware bearers cannot self-initiate break-free. Obvious conditions default True; subtle ones default False. Flipped by a successful reveal_condition check.",
            ),
        ),
        migrations.AddField(
            model_name="conditioninstance",
            name="pending_rally_bonus",
            field=models.PositiveSmallIntegerField(
                default=0,
                help_text="Pending helper bonus from a RallyAction, applied to the bearer's next break-free attempt and consumed (reset to 0) on use.",
            ),
        ),
        migrations.AddField(
            model_name="conditiontemplate",
            name="break_free_check_type",
            field=models.ForeignKey(
                blank=True,
                help_text="Check type the target rolls to break free. Falls back to resist_check_type when null.",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="breaks_free_conditions",
                to="checks.checktype",
            ),
        ),
        migrations.AddField(
            model_name="conditiontemplate",
            name="break_free_difficulty",
            field=models.PositiveIntegerField(
                default=10,
                help_text="Base difficulty for the break-free check (before caster level opposition).",
            ),
        ),
        migrations.AddField(
            model_name="conditiontemplate",
            name="break_free_mode",
            field=models.CharField(
                choices=[
                    ("none", "No break-free — subtle effect runs its course"),
                    ("self_initiated", "Target must choose to resist (player command)"),
                    ("periodic", "System auto-rolls break-free on each tick"),
                ],
                default="none",
                help_text="Whether and how a target can shake off this condition once applied. NONE = subtle, no shake-off; SELF_INITIATED = player command; PERIODIC = auto-roll on ticks.",
                max_length=20,
            ),
        ),
        migrations.AddField(
            model_name="conditiontemplate",
            name="subtlety",
            field=models.PositiveSmallIntegerField(
                default=0,
                help_text="How hard it is for an observer to detect this condition on a target (0 = obvious, higher = harder to detect). Drives the reveal_condition detection check difficulty.",
            ),
        ),
    ]

# Frontend Environment Variables
# These are used by the Vite dev server

# Allowed hosts for Vite dev server (for ngrok/tunneling services)
# Comma-separated list of domains that are allowed to access the dev server
# Leave empty for localhost-only access (most secure)
# Example: VITE_ALLOWED_HOSTS="abc123.ngrok-free.app,xyz456.ngrok.io"
# IMPORTANT: Only set this in local development, NEVER commit actual ngrok URLs
VITE_ALLOWED_HOSTS=

# Django backend port (optional, defaults to 4001)
DJANGO_PORT=4001
